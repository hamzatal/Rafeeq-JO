# 10 — مطابقة شاشات Stitch (خريطة التنفيذ التفصيلية)

> تتبّع تفصيلي لمطابقة **كل** شاشة في تطبيقات رفيق لتصميم Stitch (المرجع: `stitch_rafeeq_ai_student_platform.zip`).
> لكل شاشة: مرجع Stitch، الملف في الكود، العناصر الملزمة، الأنيميشن/التفاعل، والحالة.
> الحالة: ✅ منجزة · 🔄 جارية · ⬜ لم تبدأ.

## اللوجو (موحّد على التطبيقات الثلاثة)
شعار **R** التيل (`r-logo.png`) هو الهوية الوحيدة. مكوّن `Logo` (`mark` / `full` / `stacked`) في `student-app`, `driver-app` (+ `admin-dashboard/Logo.tsx`). اللوجوهات القديمة الذهبية/الخضراء (`logo-full.svg`, `logo-mark.svg`) **مهجورة** (تُحذف عند التنظيف النهائي).

## مبادئ المطابقة (ملزمة لكل شاشة)
- **نفس البنية والترتيب والتنسيق** كما في Stitch بالحرف (لا نكتفي بالألوان).
- **أنيميشن وتفاعل حقيقي:** دخول ناعم للعناصر، `PressableScale` (active:scale-95)، أزرار بظل يتغيّر عند الضغط، نبض تيل لعناصر AI/الحيّ.
- **RTL** كامل، خط IBM Plex Sans Arabic، ألوان كحلي #002045 + تيل #006A65.
- **بلا نصوص عربية مكتوبة في المكوّنات** — تمرّر عبر i18n.

---

## تطبيق الطالب (Student)

| الجزء | شاشة Stitch | الملف | العناصر الملزمة | الحالة |
|-------|-------------|-------|------------------|--------|
| 2.1 شل التنقّل | تبويب سفلي في `_15` | `TabBar.tsx` + `(app)/_layout.tsx` | تبويب سفلي أبيض + **زر رفيق AI مركزي عائم (دائرة كحلية)**، ترتيب: الرئيسية·الرحلات·[AI]·المحفظة·الملف | ✅ (RFQ-294) |
| 2.2 الرئيسية | `_15` (Main Dashboard) | `(app)/home.tsx` | خريطة كاملة + تدرّج سفلي؛ **بطاقة ترحيب زجاجية** (أفاتار + الاسم + تحية الوقت)؛ زر جرس زجاجي + نقطة؛ **علامة سيارة نابضة + شارة ETA**؛ **بطاقة نقاط كحلية متدرّجة** (أيقونة تيل + النقاط + المستوى)؛ **لوحة بحث زجاجية** «إلى أين الوجهة اليوم؟» + زر بحث (أيقونة تيل) + إجراءان سريعان (المنزل/الجامعة). أنيميشن: دخول المنطقة السفلية + نبض العلامة + ضغط تفاعلي | ✅ (RFQ-295) · **pixel RFQ-353** (توكنز Material-3 دقيقة) |
| 2.3 اختيار الفئة | `_16` (Taxi Selection) | `(app)/ride-request.tsx` | «اختر فئة السيارة»: AppBar (سهم رجوع RTL + رفيق 32px) + خريطة+علامات + شيت سفلي (مقبض + عنوان + 3 بطاقات فئة: iconBox 64×48 + radio ring يسار + ETA·ركّاب + سعر) + صفّا الوجهة/الدفع (iconBox 32 + chevron) + زر تأكيد لاصق. ملاحظة: تسعير الفئات النهائي في المرحلة 3 | ✅ (RFQ-329) · **pixel RFQ-354** (بنية _16 حرفية) |
| 2.3 سجل الرحلات | `_17` | `(app)/trips.tsx` | رأس (**أفاتار يمين · رفيق · جرس يسار**) + «سجل الرحلات» headline-md + **شرائح فلتر داخل صفّ العنوان** (مكتملة primary / ملغاة border) + بطاقات السجل (أفاتار السائق 48 + اسم المسار + الأجرة من pricing + شارة حالة check/cancel + خط مسار: نقطة تيل→نقطة كحلية بأيقونة موقع + تقييم نجوم + إعادة حجز/تحميل الفاتورة؛ الملغاة opacity-75). رحلاتك الحالية (تتبّع/كود/خريطة حيّة) محفوظة أعلى | ✅ (RFQ-334) · **pixel RFQ-355** (بنية _17 حرفية) |
| 2.3 المحفظة | `_18` (Student Wallet) | `(app)/wallet.tsx` | رأس (**أفاتار يمين · رفيق · جرس يسار**) + بطاقة رصيد (blob زخرفي top-right + JOD صغير) + زر شحن + **بطاقة طلب دفع كليك** (أيقونة QR في دائرة تيل ساطع يمين + مبلغ بـ JOD يمين + إنشاء رابط) + «سجل الحركات» + عرض الكل + الحركات (أيقونات ملوّنة + مبالغ) + دليل الدفع | ✅ (RFQ-330) · **pixel RFQ-354** (بنية _18 حرفية) |
| 2.3 الإعدادات والدعم | `_19` | `(app)/settings.tsx` | رأس (**أفاتار يمين · رفيق · جرس يسار**) + الإعدادات العامة (لغة ع/EN + إشعارات + طوارئ بطاقة حمراء) + مركز الدعم (اتصل/أسئلة/**مفقودات AI بطاقة كحلية** — 3 بطاقات مكدّسة) + قانوني (خصوصية/شروط + external-link) + تسجيل خروج (pill مركزي). **لا مبدّل ثيم — غير موجود في Stitch _19** | ✅ (RFQ-332) · **pixel RFQ-354** (بنية _19 حرفية) |
| 2.3 دفع الاشتراك | `_10` | `(app)/checkout.tsx` | **AppBar** (سهم رجوع يمين + العنوان + help يسار) + بطاقة الطلب (أيقونة إيصال دائرية يمين + رقم الطلب + صفّ: المبلغ 40px يسارَه الصلاحية تيل) + تعليمات الدفع (أيقونة يمين + Alias يمين + زر نسخ دائري يسار) + إثبات الدفع (رفع + معاينة + ملاحظة تحقّق AI بحافة تيل يمين) + زر تأكيد لاصق (معطّل حتى رفع الإيصال). (اختيار محفظة/CliQ محفوظ) | ✅ (RFQ-333) · **pixel RFQ-355** (بنية _10 حرفية) |
| 2.3 المفقودات الذكية | `_13` | `(app)/lost-found.tsx` | «المفقودات الذكية» display-lg + وصف (محاذاة يمين) + تبديل أضعت/وجدت + نموذج (اسم/موقع/وقت/وصف/رفع صورة) + **بلاغاتي النشطة** (أيقونة surface-container-highest + شارات حالة: جاري البحث/محتمل تطابق) + عرض السجل الكامل | ✅ (RFQ-330) · **pixel RFQ-355** (بنية _13 حرفية) |
| 2.3 مساعد رفيق AI | قسم AI في `_2` | `(app)/assistant.tsx` | رأس AI + نقطة تيل نابضة (متصل) + شريط متدرّج + فقاعات (AI كحلي/المستخدم رمادي) + مقترحات + مؤشر «يفكّر» نابض + إدخال/إرسال | ✅ (RFQ-331) |
| 2.3 الرئيسية-الخدمات | `_2` (Services grid) | (قسم/تبويب خدمات) | شبكة 2×2 (النقل الجامعي/الأغراض/المفقودات/التبادل) + بطاقة نقاط + قسم AI | ⬜ |

## تطبيق الكابتن (Driver)

| الجزء | شاشة Stitch | الملف | العناصر الملزمة | الحالة |
|-------|-------------|-------|------------------|--------|
| 2.4 لوحة القيادة | `_20` / `_3` | `(app)/dashboard.tsx` + `TabBar` | **بطاقة حالة موحّدة** (التقييم/الدخل اليومي/الرحلات + مفتاح متصل تيل) + خريطة + عروض. تبويب الكابتن مطابق (نشط كحلي + مؤشر تيل، بلا زر مركزي). بطاقة «طلب رحلة جديد» في offers | ✅ (RFQ-333) |
| 2.4 سجل الرحلات | `_21` / `_4` | `(app)/trips.tsx` | رأس + جدولة رحلة (مسار + وقت) + قائمة الرحلات (حالة + ركّاب) | ✅ (RFQ-335) |
| 2.4 المحفظة | `_22` | `(app)/earnings.tsx` + `withdraw` | رأس + **بطاقة رصيد كحلية** + زر سحب تيل + بطاقة معلومات CliQ (Alias) + المعاملات الأخيرة (تيل أرباح/كحلي سحب) | ✅ (RFQ-334) |
| 2.4 الإعدادات | `_23` | `(app)/settings.tsx` | رأس + عامة (لغة/وضع) + الحساب (مستندات/مركبة/محفظة) + مركز الدعم + قانوني + خروج | ✅ (RFQ-335) |
| 2.4 الملف الشخصي | `_24` | `(app)/profile.tsx` (جديد) + `vehicle`/`documents` | بطاقة ملف (اسم + كابتن موثّق + شارة تحقّق) + بطاقتا إحصاء (التقييم/إجمالي الرحلات) + **تفاصيل المركبة** + **المستندات الرسمية** (شارات حالة) | ✅ (RFQ-334) |
| شل التنقّل | تبويب `_20` | `driver-app/TabBar.tsx` + `(app)/_layout` | تبويب سفلي (5): الرئيسية·الرحلات·المحفظة·الملف·الإعدادات، نشط كحلي + مؤشر تيل (أُعيد الترتيب + أُضيف تبويب الملف) | ✅ (RFQ-334) |

## مشترك (Auth / Onboarding / Splash)

| شاشة Stitch | الملف | الحالة |
|-------------|-------|--------|
| Splash `_1` | `BrandSplash.tsx` (كحلي + لوجو R) | ✅ |
| Onboarding `1,2,3` | `(onboarding)/intro.tsx` (لايت Stitch أصلاً) | ✅ |
| إنشاء حساب `_14` | `(auth)/register.tsx` + `AuthShell.tsx` (لايت Stitch) | ✅ (RFQ-327/328) |
| دخول / OTP / نسيت | `(auth)/login·otp·forgot` (لايت + دخول تجريبي) | ✅ (RFQ-327/328) |

> **إصلاح حرج (RFQ-327/328):** كل شاشات الدخول في التطبيقين (الطالب + الكابتن) حُوّلت من خلفية داكنة `#0A0D12` إلى **Stitch لايت**، وأُضيف **دخول تجريبي** (`__DEV__`) للمعاينة بدون خادم. الافتراضي عربي + Light مؤكّد.

## لوحة الإدارة (Admin) — المرحلة 2.5
> **مطابِقة أصلاً لـ Stitch** عبر توكنز المرحلة 1 (Sidebar كحلي `bg-navy` يمين RTL + تيل `cyan→teal` + خط IBM Plex + بطاقات/جداول لايت). البنية تطابق `_26` (KPIs + رسم بياني + جدول نشاطات + Sidebar).

| شاشة Stitch | الملف | الحالة |
|-------------|-------|--------|
| Sidebar يمين RTL (كحلي+تيل) | `Sidebar.tsx` + `(dashboard)/layout` | ✅ (توكنز م.1) |
| التحليلات `_26` | `app/(dashboard)/page.tsx` (KPIs + chart + disputes) | ✅ |
| إدارة الطلاب `_25` | `.../users` | ✅ (توكنز) |
| إدارة الكباتن `_27` | `.../drivers` | ✅ (توكنز) |
| الرحلات `_28` | `.../trips` | ✅ (توكنز) |
| المالية `_29` | `.../payments` · `cliq` · `reports` | ✅ (توكنز) |

> ملاحظة: صفحات الأدمن تستخدم مكوّنات مشتركة (`kpi-card`,`data-table`,`pill-*`,`nav-item`) معرّفة في `globals.css` وكلها على توكنز Stitch. أي ضبط دقيق مستقبلي = تجميلي فقط.
