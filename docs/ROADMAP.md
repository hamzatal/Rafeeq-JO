# خطة العمل التفصيلية — رفيق

> **المصدر الوحيد للحقيقة.** أي عمل غير موجود هنا لا يُنفَّذ.
> كل مهمة لها: **رقم · وصف · الملفات · معيار القبول · التبعية · الحجم**
> الحجم: `S` ≤ نصف يوم · `M` يوم · `L` يومان · `XL` 3+ أيام

---

## القرارات المعتمدة

| # | القرار | الحالة |
|---|---|---|
| 1 | **مسار الرحلة:** الطلب الفوري (`RideRequests` + `Matching`) هو المنتج. `Trips` المجدولة = «خطّي الشهري» بتدفّق منفصل | ✅ |
| 2 | **الهوية:** «مسار» · أزرق `#1259E3` · بلا لون ثانوي · فاتح فقط | ✅ |
| 3 | **الشعار:** «الطريق هو الحرف» · القفلة (أ) علامة + كلمة | ✅ |
| 4 | **Splash:** فاتح | ✅ |
| 5 | **سايدبار الأدمن:** فاتح | ✅ |
| 6 | **الكثافة:** المقياس المكثَّف · لمس 44+ (54 في وضع الرحلة) | ✅ |
| 7 | **الوضع الداكن:** يُحذف | ✅ |
| 8 | **الأيقونات:** Lucide حصراً | ✅ |
| 9 | **السنّ الأدنى: 18 سنة** (سنّ الرشد الأردني) — يُمنع من هم تحتها | ✅ |
| 10 | **الإلغاء بعد الصعود:** بعد `capture` لا إلغاء — «مشكلة في الرحلة» تفتح نزاعاً | ✅ |
| 11 | **نموذج التسعير:** «المقعد الثابت» — مصفوفة (منطقة↔جامعة) بلا surge | ⚠️ **7 أرقام تحتاج حسمك** → [`PRICING.md`](PRICING.md) §7 |
| 12 | **الشِعار:** «مقعدك إلى الجامعة» · الوعد «تدفع مقعداً، لا رحلة» | ✅ |
| 13 | **نقطة وجهة الشعار كهرمانية** `#F59E0B` — الاستخدام الوحيد المسموح للون ثانٍ | ✅ |
| 14 | **كود الصعود/الإنزال: 4 أرقام** (وهو ما يولّده الكود أصلاً) — و**throttle إلزامي** لا اختياري، لأنّ 4 أرقام = 10,000 احتمال | ✅ القرار · ⏳ الـthrottle بند 1.20 |
| 15 | **السبلاش:** خريطة باهتة + العلامة + الشِعار · فاتح للطالب، داكن للكابتن | ✅ |
| 16 | **بار التنقّل:** مرفوع بظلّ لا حدّ · كبسولة `brand-600` مصمتة وأيقونة مصمتة للنشط | ✅ |
| 17 | **التعرفة بيانات لا كود** — جدول بنسخة وتاريخ إقرار ومرجع قرار، لأنّ الهيئة تُقرّها لا نحن | ✅ القرار |
| 18 | **المضاعف الليلي 1.25 يُحذف** — «تجاوز التعرفة» مخالفة عقوبتها تصل لسحب الرخصة | ✅ القرار · ⏳ بند 5.0 |
| 19 | **هيكل المحفظة: عميل لمرخَّص من البنك المركزي لا دفتر أستاذ داخلي** | ⚠️ **محجوب على سؤال المحامي 3** |

> **⚠️ قرار مُعلَّق يحجب تصميماً:** نموذج التجميع قانونياً أقرب إلى **«خطّ نقل عام منتظم»**
> من «تطبيق نقل ركاب»، والرخصتان مختلفتان. التفصيل والأسئلة التسع في
> [`legal/JORDAN-COMPLIANCE.md`](legal/JORDAN-COMPLIANCE.md) — **الأسئلة 1–4 تحجب تصميماً لا تنفيذاً.**

## القواعد

1. فرع واحد `main` · كل عمل في `feat/`·`fix/`·`chore/`·`docs/` وينتهي بـPR وحذف تلقائي
2. **رقم Issue** هو المعرّف — لا `RFQ-###` جديد (RFQ‑366 الأخير)
3. **CI أخضر إلزامي** — بلا استثناء، بلا `--no-verify`
4. **كل إصلاح مالي أو أمني يحتاج اختباراً يفشل قبله وينجح بعده**
5. لا مرحلة تبدأ قبل إغلاق بوابة التي قبلها
6. الحذف قبل الإضافة

---

# ✅ المرحلة 0 — المنجز

| # | المهمة | النتيجة |
|---|---|---|
| 0.1 | إصلاح CI الأحمر — تعارض `@types/react` (`^18.3.3` مقابل override `18.2.79`) أنشأ نسخة متداخلة `18.3.31`، وأنواع React 19 أضافت `bigint` لـ`ReactNode`. + تحويل CI والصورة إلى `npm ci` | **PR #66 · `main` أخضر** |
| 0.2 | ضمّ `foundation` إلى `main` · حذف **54 فرعاً** (66→9) · `main` صار الافتراضي | ✅ |
| 0.3 | حذف الـzip 6.8MB · `splash-map.jpg` ×2 · `SECURITY_QA_AUDIT.md` · 6 صفائح متجاوزة | ✅ |
| 0.4 | توحيد الوثائق: 20 ملفاً → **5** | ✅ |

---

# 🔴 المرحلة 1 — إسعاف مالي وأمني · 7 أيام

**لماذا أولاً:** خسارة مالية **جارية الآن** + ثغرة استخراج أموال.

| # | المهمة | الملفات | معيار القبول | تبعية | حجم |
|---|---|---|---|---|---|
| **1.1** | **افصل الواجبات المالية** — انزع `payments.approve` من `supervisor`؛ أنشئ `wallet.credit` و`payouts.approve` منفصلتين **لأدمن فقط** | `RolesPermissionsSeeder.php:87` · `Wallet/Routes/api.php` · `Payouts/Routes/api.php` | اختبار: supervisor يحصل 403 على شحن المحفظة واعتماد السحب | — | M |
| **1.2** | **ضوابط شحن المحفظة** — حدّ أقصى إعدادي · `reason` **إلزامي** · موافقة ثانية فوق حدّ معيّن · `confirm` في الواجهة | `WalletController.php:61` · `users/page.tsx:145-160` | اختبار: مبلغ فوق الحدّ يُرفض · طلب بلا سبب يُرفض | 1.1 | M |
| **1.3** | **حرس الأدمن** — امنع تعديل الذات وامنع إزالة آخر أدمن | `StaffService.php:56-94` | اختبار: الأدمن الوحيد لا يستطيع تخفيض نفسه | — | S |
| **1.4** | **عرّف `trips.view`** أو استبدل الفحص بالدور | `RolesPermissionsSeeder` · `TripChannelPolicy.php:23` | supervisor يشترك في قناة الرحلة | — | S |
| **1.5** | **رحلة بلا كابتن = خطأ صريح** لا ائتمان صامت بصفر | `RideBillingService.php:93-105` | اختبار: `capture` بلا كابتن يرمي ويُلفّ | — | M |
| **1.6** | **طبّع المنطقة الزمنية عند الإدخال** | `TripService.php:41-52` · `RideRequestService.php:57` | اختبار: إرسال `21:30Z+3` يُقرأ 21:30 عمّان | — | M |
| **1.7** | **`cancelBooking` يفكّ الحجز المالي** — انقله للخدمة داخل transaction + `lockForUpdate` · أعِد الطلب لـ`Pending` · أعِد رصيد الاشتراك | `StudentTripController.php:52-60` · `TripService` | اختبار: بعد الإلغاء `held_fils = 0` والطالب يستطيع طلباً جديداً | — | L |
| **1.8** | **امنع إلغاء رحلة بعد أول `capture`** + احرس ضد الإلغاء المتكرر | `TripService.php:224-288` | اختبار: إلغاء رحلة فيها راكب مدفوع يُرفض | 1.7 | M |
| **1.9** | **`lockForUpdate` في كل موديول `Trips`** — السعة · الصعود · البدء/الإنهاء | `TripService.php:291,326,64,124` | اختبار تزامن (يتطلّب 1.13) | 1.13 | L |
| **1.10** | **قفل `payWithWallet`** + مسار احتياطي للمحفظة عند انتهاء الاشتراك | `SubscriptionService.php:70-118` | اختبار: نقرتان متزامنتان = خصم واحد | 1.13 | M |
| **1.11** | **أوقف بثّ الموقع عند الخروج** + امسح العلم من AsyncStorage | `driver-app/store/availability.ts:33-49` · `auth.ts:116` | بعد الخروج: صفر نداء موقع (مُختبَر يدوياً) | — | S |
| **1.12** | **توكن الأدمن إلى `httpOnly` cookie** | `admin-dashboard/src/lib/api.ts:3-9` · `auth.tsx` | `localStorage` خالٍ من التوكن | — | L |
| **1.13** | **بيئة اختبار حقيقية** — Postgres في `phpunit.xml` بدل SQLite `:memory:` + تفعيل `<coverage>` + `failOnRisky`/`failOnSkipped` | `phpunit.xml` · `ci.yml` | `lockForUpdate` يعمل فعلاً في الاختبار · CI يعرض نسبة تغطية | — | M |
| **1.14** | احذف `otp_debug` من كل الاستجابات · `app()->isProduction()` · افتراضي `false` · احذف `universal_code` | `config/otp.php` · `OtpService.php:68` · `AuthController.php` | اختبار: الاستجابة لا تحتوي الرمز في أي بيئة | — | S |
| **1.15** | لا تسجّل نصّ رسالة SMS · استثنِ `LogSmsGateway` من fallback الإنتاج | `LogSmsGateway.php:15` · `FallbackSmsGateway.php:26` | اللوج لا يحتوي رمزاً ولا هاتفاً | — | S |
| **1.16** | `lockForUpdate` في `PayoutService::approve/reject` | `PayoutService.php:73-142` | اختبار تزامن: أحدهما يفشل | 1.13 | S |
| **1.17** | احذف `devLogin` من كود الإنتاج | كلا `store/auth.ts` | صفر مرجع | — | S |
| **1.18** | افصل مفتاح خرائط العميل عن السيرفر + قيّده | `routes/api.php:71-84` | `/config` لا يُرجع مفتاح السيرفر | — | S |
| **1.20** | **throttle على الصعود/الإنزال** — 4 أرقام = 10,000 احتمال، وبلا حدّ للمحاولات يستطيع الكابتن تأكيد إنزال راكب لم ينزل بالتخمين، وهذا يهزم ضابط «التأكيد من الطرفين» الذي يبني عليه مركز النزاعات | `Trips/Routes/api.php:8-21` | اختبار: 5 محاولات خاطئة تُقفل المسار · سجلّ تدقيق للمحاولة الفاشلة | — | S |
| **1.19** | **العملة بثلاث منازل لا اثنتين** — `toFixed(2)` على قيم بالفلس **يُدوِّر ويعرض مبلغاً خطأ** (`1.999`→`2.00`، `1.005`→`1.00`)، وأخطر موقعَين هما صفحتا **تحديد** السعر | `coupons:77` · `plans:101` · `routes:108` · `zone-prices:133` | اختبار: 1999 فلساً تُعرض `1.999` · بوابة CI تمنع `toFixed(2)` على قيمة مالية | — | S |
| **1.19** | سقف مبلغ للاعتماد الآلي لدفعات CliQ | `PaymentService.php:188-228` | فوق السقف ⇒ مراجعة بشرية | — | S |
| **1.20** | `confirm` + سبب إلزامي على: اعتماد دفعة · **اعتماد سحب** · تجميد حساب | `payments:148` · `withdrawals:119` · `disputes:181` | لا إجراء مالي بنقرة واحدة | — | M |
| **1.21** | نظّف `.env.example` (`APP_DEBUG=false` · أسرار `CHANGE_ME`) | `backend/.env.example` | لا سرّ حقيقي المظهر | — | S |

**🚪 بوابة 1:** لا يمكن الحصول على OTP من الشبكة أو اللوج · supervisor محجوب عن المال · `approve`+`reject` متزامنان أحدهما يفشل · إلغاء حجز يفكّ الحجز · رحلة 21:30 تُقرأ 21:30 · **CI يعرض نسبة تغطية**.

---

# 🔴 المرحلة 2 — الامتثال والقانون · 6 أيام

**لماذا الآن:** 10 بنود **رافضة** من المتاجر + تصريح خصوصية يخالف الكود.

| # | المهمة | الملفات | معيار القبول | حجم |
|---|---|---|---|---|
| **2.1** | **ترقية Expo SDK 51 → 54+** (targetSdk 35) · `expo-updates` · `versionCode`/`buildNumber` · معالجة `newArchEnabled` | كلا `package.json` · `app.json` · `eas.json` | `eas build --profile production` ينجح · Play يقبل targetSdk | XL |
| **2.2** | **`expo-location` في `driver-app`** — مثبّتة ومعلَنة في `plugins` | `driver-app/package.json` · `app.json` | تتبّع الكابتن يعمل على جهاز حقيقي | S |
| **2.3** | **`apiUrl` إنتاجي** في `eas.json` | `eas.json` ×2 | بناء إنتاجي يتصل بالـAPI الحقيقي | S |
| **2.4** | **أيقونة 1024** + `splash.image` + `adaptiveIcon.foregroundImage` — من `docs/design/v2/00-logo.png` | `app.json` ×2 · `packages/ui/brand` | تظهر على جهاز حقيقي | M |
| **2.5** | **`locationWhenInUse`** بدل `Always` · مبرّرات الكاميرا/الصور بالعربي · `android.permissions` صريحة · احذف `expo-document-picker` غير المستخدم | `app.json` ×2 | لا إذن بلا استخدام فعلي | M |
| **2.6** | **شاشة حذف الحساب** + `anonymizeAccount()` حقيقي (استبدال الاسم/الهاتف/البريد + حذف الوثائق) | `settings` ×2 · `ProfileService.php:85-90` | حذف يعمل من الواجهة ويُجهِّل فعلاً (مُختبَر) | L |
| **2.7** | **انشر الخصوصية والشروط على URL عام** واربطها فعلياً | `settings.tsx:91-92` · `driver settings:55` | الزرّان يفتحان مستنداً حقيقياً | M |
| **2.8** | **أعِد كتابة `privacy-ar.md`** — المعالجات الفرعية (OpenAI · Google · FCM · WhatsApp) · نقل البيانات خارج الأردن · الأساس القانوني · جهة حماية البيانات · الكوكيز · القاصرون · القرارات الآلية وحق الطعن | `docs/legal/privacy-ar.md` | كل معالج في الكود مذكور في الوثيقة | L |
| **2.9** | **أعِد كتابة `terms-ar.md`** — **سنّ 18** · سياسة الاسترجاع · العمولة وآلية تغييرها · غرامات No-show بأرقام · إصدار الشروط و`terms_accepted_at` | `docs/legal/terms-ar.md` · هجرة | كل عملية مالية في الكود لها أساس تعاقدي معلن | L |
| **2.10** | **تحقق العمر 18** — حقل تاريخ ميلاد + تحقق باكند + منع من هم تحتها | `RegisterRequest.php` · هجرة · شاشة 06 | اختبار: مولود قبل 18 سنة يُرفض | M |
| **2.11** | **تنقيح الإيصال قبل OpenAI** + موافقة صريحة + إلغاء الرابط العام (base64 فقط) | `PaymentService.php:483-495` | لا رابط عام لأي إيصال · موافقة مُسجَّلة | L |
| **2.12** | **اربط `/admin/safety/*`** بصفحة SOS | `Safety/Routes/api.php:31-37` · `admin/safety` | بلاغ SOS يظهر خلال ثوانٍ | M |
| **2.13** | **احذف البيانات الوهمية** — «٣ دقائق» · السيارة الوهمية · أشرطة KPI الثابتة | `home.tsx:83-92` · `admin/page.tsx:90-121` | صفر بيان غير حقيقي | S |
| **2.14** | **تقنيع PII** — هواتف `07XXXXX89` + صلاحية `users.view_pii` منفصلة + وثائق الهوية بمعاينة داخلية لا `window.open` | `users/page.tsx:92` · `drivers/[id]:40` | `support` لا يرى هاتفاً كاملاً | M |
| **2.15** | **نظّف نصوص الإشعارات** — احذف اسم المُرسِل ونصّ المحادثة · احذف اسم ضحية SOS من SMS | `ChatService.php:133` · `SosService.php:71` | لا PII في أي `notification.body` | M |

**🚪 بوابة 2:** التطبيقان يُبنيان إنتاجياً ويُقبلان في مراجعة داخلية لكل بنود المتاجر · كل معالج فرعي مُفصَح · حذف الحساب يعمل · سنّ 18 مفروض.

---


### مضاف من الدراسة القانونية الأردنية → [`legal/JORDAN-COMPLIANCE.md`](legal/JORDAN-COMPLIANCE.md)

| # | المهمة | معيار القبول | حجم |
|---|---|---|---|
| **2.0** | **قرار هيكل المحفظة** — يُتّخذ **قبل** إكمال بنود المحفظة في المرحلة 1. حفظ رصيد العميل يحتاج ترخيص بنك مركزي، فإمّا عميل لمرخَّص أو لا محفظة | قرار موثَّق + مخطّط ترحيل `Modules/Wallet` | M |
| **2.1** | **ملفّ الأسئلة التسع للمحامي** مع حزمة وقائع (مقتطفات الكود + النموذج + مصفوفة التعرفة) | مُرسَل ومُجاب على 1–4 على الأقل | S |
| **2.2** | **تكامل LTRC** — بثّ GPS والدفع إلى الهيئة · حقل رخصة LTRC للكابتن · بطاقة QR وعرضها | الرحلة لا تبدأ لكابتن بلا رقم رخصة سارٍ | XL |
| **2.3** | **سجلّ الفحص اليومي للمركبة** (المادة 12) + الإسعافات والطفّاية في قائمة التوثيق | لا اتصال بلا فحص اليوم | M |
| **2.4** | **اربط الرحلة بالمركبة المصرَّح بها** وارفض غيرها (المادة 12) | محاولة بمركبة أخرى تُرفض وتُسجَّل | S |
| **2.5** | **مراجعة `Coupons` قانونياً** — الخصم قد يكون «تخفيض أجرة دون موافقة» | قرار موثَّق: يبقى · يُقيَّد · يُحذف | S |
| **2.6** | **تقنيع PII وحدّ أدنى للوصول** — الهاتف مقنَّع لـ`support` · وثائق الهوية برابط موقَّع قصير العمر لا `window.open` | `support` لا يرى هاتفاً كاملاً · لا وثيقة في تبويب متصفح | M |
| **2.7** | **وائم `privacy-ar.md` مع الكود** — أحدهما يجب أن يتغيّر، والكود هو الحقيقة | صفر تصريح لا يقابله كود | M |
| **2.8** | **DPO + التسجيل لدى MoDEE + سجلّ المعالجة** | مُسجَّل | M |
| **2.9** | **قرّب موقع الطالب إلى نقطة تحميل** حيث لا تلزم دقّة الباب | لا إحداثيّة باب منزل مخزَّنة | M |
| **2.10** | **أصلح تسريبات الإشعارات** — لا نصّ محادثة، ولا اسم ضحية SOS لكل الموظفين | اختبار: نصّ الإشعار بلا محتوى ولا اسم | S |

# 🟠 المرحلة 3 — الخصوصية وسلامة الدفتر · 5 أيام

| # | المهمة | معيار القبول | حجم |
|---|---|---|---|
| **3.1** | أصلح الاحتساب المزدوج — رحلة الاشتراك لا تُقيَّد كعمولة رحلة · الكوبون يُفسَّر في التقرير | اختبار: `gross = commission + captain_share + discount` | L |
| **3.2** | `restrictOnDelete` على كل الجداول المالية بدل `cascade` على `users` | حذف مستخدم يفشل إن كان له دفتر | M |
| **3.3** | `CHECK (x >= 0)` على كل أعمدة المال | إدخال سالب يفشل على مستوى القاعدة | S |
| **3.4** | `PruneDriverLocations` (7 أيام) + إصلاح `PruneTripTracking` ليشمل الرحلات العالقة | صفر جدول تتبّع بلا سقف | M |
| **3.5** | `PruneDriverDocuments` — حذف عند الرفض/الاستقالة/حذف الحساب | وثائق مرفوضة تُحذف فعلاً | M |
| **3.6** | `PruneTickets` + `PrunePayments` — التزامان معلنان وغير منفَّذين | كل التزام في وثيقة الاحتفاظ له job | M |
| **3.7** | `ExpireSubscriptions` + فهرس `ends_at` + `endOfDay` | صفر اشتراك منتهٍ بحالة `active` | M |
| **3.8** | تشفير الهاتف/الاسم/العناوين + فهرس hash للبحث | البحث بالهاتف يعمل والعمود مشفَّر | L |
| **3.9** | **نسخ احتياطي `pg_dump` يومي + خارج الموقع + اختبار استعادة شهري مؤتمَت** | **استعادة ناجحة مُختبَرة مرة على الأقل** | L |
| **3.10** | healthcheck حقيقي للـ`scheduler` (اليوم `\|\| exit 0`) + تنبيه `failed_jobs` + `failed()` للـjobs | قتل العامل يُشعل تنبيهاً | M |
| **3.11** | مفاتيح أجنبية للمراجع «المنطقية» — أهمها `trips.zone_id` | صفر مرجع يتيم | M |
| **3.12** | حدّ/تقسيم على استعلام المطابقة + chunking في `broadcast` و`alertSafetyTeam` | 10,000 طلب معلّق لا يُفلح الذاكرة | M |
| **3.13** | تقليم `audit_logs` · `notifications` · `chat_messages` · `ai_messages` | كل جدول ينمو له سياسة | M |

**🚪 بوابة 3:** 6/6 التزامات احتفاظ منفَّذة · استعادة نسخة مُختبَرة · صفر جدول بلا سقف.

---

# ✂️ المرحلة 4 — الحذف الكبير · 5 أيام

| # | المهمة | الحجم |
|---|---|---|
| **4.1** | احذف `Modules/Areas` + `Modules/PickupPoints` (~1336 سطر) | S |
| **4.2** | احذف 7 شاشات ميتة (791 سطر) | S |
| **4.3** | احذف 38 دالة api-client بلا مستدعي + `ServiceTile` + `POST /wallet/topup-instructions` + `AdBanner` من الرئيسية | M |
| **4.4** | احذف اللوحة الداكنة + 57 `dark:` + الأسماء المستعارة `gold/cyan/blue/navy` + تعليقات الهويات الميتة | M |
| **4.5** | **أكمل `support.show`/`reply`** ثم ادمج `Complaints` في `Support` | L |
| **4.6** | صِل `PATCH/DELETE /driver/vehicles/{id}` و`ratings.mine/received` | M |
| **4.7** | أجّل `Exchange`/`Parcels`/`LostFound` إلى `parking/marketplace` | S |
| **4.8** | سكربت CI: يفشل عند أي شاشة بلا مسار وصول أو دالة API بلا مستدعي | M |

**🚪 بوابة 4:** 20 موديولاً · صفر شاشة ميتة · صفر دالة بلا مستدعي.

---

# 💰 المرحلة 5 — محرك التسعير · 5 أيام

**⚠️ محجوبة حتى تحسم 7 أرقام في [`PRICING.md`](PRICING.md) §7**

| # | المهمة | حجم |
|---|---|---|
| **5.1** | أعِد كتابة `PricingService` — احذف surge والتعريفة الليلية والتسعير بالدقيقة · نطاقات ثابتة · معامل المنفرد | L |
| **5.2** | نافذة تجميع في `MatchingService` + حدّ أدنى للامتلاء قبل الإرسال | L |
| **5.3** | ضمان الحدّ الأدنى للكابتن بسقف يومي — يُدفع من العمولة لا من الطالب | M |
| **5.4** | مصفوفة (منطقة↔جامعة) كمصدر السعر + أداة توليد من المسافة في الأدمن | M |
| **5.5** | حسم تناقض العمولة: `config` يقول 15% والأدمن يعرض 12% | S |
| **5.6** | أصلح تسرّب التقريب (`intdiv`) ودقّة الفلس (`toFixed(2)` تبتر خانة) | S |
| **5.7** | اختبارات: كل نطاق · المنفرد · الضمان · zero-sum · تقريب | M |

**🚪 بوابة 5:** المقعد النموذجي < 2.000 · المنفرد = المقاعد كلها · صفر surge · اختبار zero-sum أخضر.

---

# 🎨 المرحلة 6 — التوكنز · 4 أيام

| # | المهمة | حجم |
|---|---|---|
| **6.1** | `packages/tokens` من `docs/design/src/kit.css` → يولّد TS + CSS + Tailwind preset | L |
| **6.2** | احذف `packages/shared/src/theme/**` · استبدل `theme.ts` في التطبيقين | M |
| **6.3** | 3 أوزان خط فقط — **احذف `extrabold` الوهمي** (40+ استخدام) | S |
| **6.4** | Lucide في التطبيقات الثلاثة | M |
| **6.5** | **دالة عملة واحدة** `formatJod(fils)` في `packages/tokens` — تعزل الأرقام ثنائي الاتجاه وتُخرج ثلاث منازل دائماً. احذف **8** صياغات مكرّرة في `admin-dashboard/app` وكل قالب `${n} د.أ` خام | M |
| **6.6** | **عزل ثنائي الاتجاه للأرقام** — لا `unicodeBidi` ولا `writingDirection` في أي ملف حالياً. كل رقم/عملة/مدى/نسبة يمرّ بمكوّن `<Num>` | M |
| **6.7** | **7 بوابات CI:** hex يدوي (117) · حجم خط (7) · RTL (388) · `dark:` (57) · انحراف التوكنز · تباين <4.5 (1) · `toFixed(2)`/`د.أ` خام خارج `formatJod` | L |

**🚪 بوابة 6:** كل الأرقام أعلاه = **0**، ولا مبلغ يُعرض إلا عبر `formatJod`.

> **من أين جاءت 6.5 و6.6:** نفس الخطأين وقعا في مصيِّر صفائح التصميم — العملة انقلبت
> والأرقام انعكست — فدفع ذلك للبحث عنهما في كود الإنتاج فوُجدا (`AUDIT.md` خ39·خ40).

---

# 🧩 المرحلة 7 — مكتبة المكوّنات · 5 أيام

| # | المهمة | حجم |
|---|---|---|
| **7.1** | `packages/ui` + نقل `LiveMap` **مرة واحدة** (510 سطر ×2) | L |
| **7.2** | نقل المتطابقات (`Feedback` 233 · `MapBackdrop` · `Loader` · `Screen` · `Icon`) | M |
| **7.3** | وحّد 9 ملفات منحرفة + `lib/api` + `store/auth` + `i18n` | L |
| **7.4** | `Text` هو المكوّن الوحيد للنص + ESLint يمنع `<Text>` الخام · `Button` 6→4 variants | M |
| **7.5** | **`accessibilityLabel` إلزامية** على كل زر أيقونة (اليوم 0/154) + `<th scope>` في الأدمن | L |
| **7.6** | `KeyboardAvoidingView` في كل شاشة بحقول (ناقصة في 6) | S |
| **7.7** | حالات `loading`·`empty`·`error`·`offline` لكل قائمة + L0–L3 (حدّ أو ظل) | L |
| **7.8** | معالجة مركزية لـ403/422/500 + timeout منفصل للرفع + retry | M |
| **7.9** | احترام `reduce-motion` + إصلاح `useFocusEffect`+`useEffect` المزدوج + سباق `refreshDriver` | M |
| **7.10** | Vitest للمكوّنات الأساسية (اليوم **صفر اختبار فرونت**) | L |

**🚪 بوابة 7:** صفر ملف مكوّن مكرّر · 154/154 زر له تسمية · ≥40 اختبار فرونت.

---

# 📱 المرحلة 8 — تطبيق الطالب · 5 أيام
**19 → 12 شاشة · 4 تبويبات** — المرجع: `docs/design/v2/02·03·04`

| # | المهمة | حجم |
|---|---|---|
| 8.1 | الشل + التبويبات + AI يخرج من المركز إلى حبّة على الرئيسية | M |
| 8.2 | الرئيسية: خريطة + شيت واحد بثلاث نقاط توقّف | L |
| 8.3 | **«مشتركة» مقابل «منفردة»** بسعرين معلنين (بدل فئات المركبة) | L |
| 8.4 | الرحلة الحيّة + التتبّع + رمز الصعود 6 أرقام | L |
| 8.5 | المحفظة + شحن CliQ + **إكمال نوع `Wallet` بحقول التوفّر** | M |
| 8.6 | حسابي + حذف الحساب + الروابط القانونية | M |
| 8.7 | `emergency` يُبسّط من 340 سطراً · `FlashList` في كل قائمة | M |

# 🚗 المرحلة 9 — تطبيق الكابتن · 5 أيام
**14 → 8 شاشات** — المرجع: `docs/design/v2/05`

| # | المهمة | حجم |
|---|---|---|
| 9.1 | الكوكبِت + مفتاح متصل بعرض الشاشة (60) + رقم بطل واحد | M |
| 9.2 | العرض الوارد ملء الشاشة + عدّاد + صوت + اهتزاز | L |
| 9.3 | وضع الرحلة: خطوة واحدة · أهداف 54 · **بلا تمرير** · كود 6 أرقام + throttle | L |
| 9.4 | دمج 4 شاشات مالية في واحدة + **«الرصيد المتاح» يعرض المتاح فعلاً** | M |
| 9.5 | مركبتي ووثائقي + تعديل/حذف المركبة | M |

# 🖥 المرحلة 10 — لوحة الإدارة · 5 أيام
**28 → 18 صفحة · 6 → 4 مجموعات** — المرجع: `docs/design/v2/06-admin-1/2/3`

| # | المهمة | حجم |
|---|---|---|
| 10.1 | الشل: سايدبار فاتح + command palette واحدة (حذف الملاحة المزدوجة) | M |
| 10.2 | الصفحات الأربع الحاكمة: لوحة القيادة · الطلبات الحيّة · الكباتن · المدفوعات | L |
| 10.3 | السلامة و SOS · الدعم المدموج · التسعير · التدقيق | L |
| 10.4 | رأس جدول فاتح · `tabular-nums` · `ms/me/start/end` · **سقف `per_page`=100** · 3 حالات لكل جدول | M |
| 10.5 | **i18n حقيقي** (اليوم عربي مثبّت بالكامل) | L |

---

# 🛡 المرحلة 11 — التصليب · 6 أيام

| # | المهمة | حجم |
|---|---|---|
| 11.1 | `throttle:auth` مطبّع + مفتاح لكل endpoint + قفل حساب + backoff | M |
| 11.2 | توكن 7 أيام + refresh + `abilities` مقيّدة بالدور | L |
| 11.3 | **`Policy` + `scopeBindings()`** — اليوم **صفر Policy** والحماية يدوية | XL |
| 11.4 | CORS: `localhost` في `local`/`testing` فقط · أسقط `supports_credentials` | S |
| 11.5 | **reverse proxy + TLS** — كل المنافذ اليوم HTTP خام | L |
| 11.6 | `ENTRYPOINT`/`CMD` في `backend/Dockerfile` (الهجرات لا تُشغَّل اليوم) + `.dockerignore` يستثني `tests/`·`*.md` | S |
| 11.7 | حدود موارد على كل خدمة + healthcheck لـ`reverb` و`admin` | S |
| 11.8 | اختبارات: 5 موديولات صفر تغطية · **اختبار تزامن لكل مسار مالي** · عقود API/i18n بلا `skip` صامت | XL |
| 11.9 | E2E (Maestro): تسجيل→OTP→رئيسية · طلب→دفع · كابتن: قبول→بدء→إنهاء | L |
| 11.10 | لقطات بصرية لكل شاشة + اختبار تكبير خط 200% | M |

**🚪 بوابة 11:** TLS على كل نقطة عامة · `Policy` لكل موديول · 3 مسارات E2E خضراء · تغطية ≥60%.

---

# 🚀 المرحلة 12 — الإطلاق · 5 أيام

| # | المهمة |
|---|---|
| 12.1 | Staging مطابق للإنتاج + seed واقعي |
| 12.2 | **بيتا مغلقة**: 20 طالباً + 5 كباتن في إربد · أسبوع · قناة ملاحظات |
| 12.3 | إفصاح خصوصية المتجرين (Data Safety · App Privacy) مطابقاً للواقع بعد المرحلة 2 |
| 12.4 | صفحات المتجر — اللقطات تُولَّد من `docs/design/src` |
| 12.5 | مراجعة داخلية لكل بنود المتاجر |
| 12.6 | Sentry + تنبيهات (SOS مفتوح · SLA المدفوعات · فشل `scheduler`) |
| 12.7 | Runbook + خطة تراجع **مُختبَرة** |
| 12.8 | **إطلاق تدريجي: اليرموك أولاً** → أسبوعان → توسّع |

---

## الجدول الزمني

| المرحلة | المدة | مانعة للإطلاق |
|---|---|---|
| 0 — المنجز | ✅ | — |
| 1 — إسعاف مالي وأمني | 7 أيام | ✅ |
| 2 — الامتثال والقانون | 6 أيام | ✅ |
| 3 — الخصوصية والدفتر | 5 أيام | ✅ |
| 4 — الحذف | 5 أيام | ✅ |
| 5 — التسعير | 5 أيام | ✅ ⚠️ محجوبة |
| 6 — التوكنز | 4 أيام | ✅ |
| 7 — المكوّنات | 5 أيام | ✅ |
| 8 — الطالب | 5 أيام | ✅ |
| 9 — الكابتن | 5 أيام | ✅ |
| 10 — الإدارة | 5 أيام | — |
| 11 — التصليب | 6 أيام | ✅ |
| 12 — الإطلاق | 5 أيام | ✅ |

**≈ 63 يوم عمل ≈ 13 أسبوعاً.**

**المسار الحرج:** `1 → 2 → 5 → 6 → 7 → 8 → 9 → 11 → 12`
المرحلتان **3** و**4** تجريان بالتوازي مع 5–7 · المرحلة **10** بالتوازي مع 11.

---

## مؤشّرات النجاح

| المؤشّر | البداية | الآن | الهدف |
|---|---|---|---|
| CI على `main` | 🔴 | **🟢** ✅ | 🟢 |
| الفرع الافتراضي | `foundation` | **`main`** ✅ | `main` |
| فروع عن بُعد | 66 | **9** ✅ | ≤5 |
| ملفات الوثائق | 20 | **5** ✅ | ≤6 |
| وزن أصول ميتة | 7.4 MB | **0** ✅ | 0 |
| ألوان hex يدوية | 117 | 117 | **0** |
| خرق RTL | 388 | 388 | **0** |
| `dark:` | 57 | 57 | **0** |
| أسطر مكرّرة | 962 | 962 | **0** |
| موديولات | 33 | 33 | **20** |
| دوال API بلا مستدعي | 38 | 38 | **0** |
| شاشات ميتة | 7 | 7 | **0** |
| `accessibilityLabel` | 0/154 | 0/154 | **154/154** |
| اختبارات فرونت | 0 | 0 | **≥40** |
| تغطية الباكند | **غير مقيسة** | غير مقيسة | **≥60% مقيسة** |
| موديولات بلا اختبار | 5 | 5 | **0** |
| مسارات مالية بلا قفل | 5 | 5 | **0** |
| أدوار تكسر فصل الواجبات | 1 | 1 | **0** |
| التزامات احتفاظ | 2/6 | 2/6 | **6/6** |
| نسخ احتياطي مُختبَر | ✗ | ✗ | **شهري** |
| بنود امتثال مفتوحة | 10 | 10 | **0** |
| معالجات فرعية مُفصَحة | 0/4 | 0/4 | **4/4** |

---

## ما ينتظر قرارك

| # | القرار | يحجب |
|---|---|---|
| 1 | **7 أرقام التسعير** → [`PRICING.md`](PRICING.md) §7 (النطاقات · معامل المنفرد · نافذة التجميع · الضمان · العمولة · الرحلة العكسية · الاشتراك) | **المرحلة 5** |
| 2 | **مراجعة قانونية أردنية** للوثيقتين بعد إعادة كتابتهما | بوابة المرحلة 2 |

---

## الملاحق
- [`AUDIT.md`](AUDIT.md) — التدقيق الشامل (5 جولات)
- [`PRICING.md`](PRICING.md) — نموذج التسعير
- [`design/SCREENS.md`](design/SCREENS.md) — 41 واجهة
- [`design/README.md`](design/README.md) — الهوية
- [`design/src/`](design/src/) — مصادر الهوية القابلة لإعادة التوليد
