# خطة العمل التفصيلية — رفيق

> **المصدر الوحيد للحقيقة.** أي عمل غير موجود هنا لا يُنفَّذ.
> كل مهمة لها: **رقم · وصف · الملفات · معيار القبول · التبعية · الحجم**
> الحجم: `S` ≤ نصف يوم · `M` يوم · `L` يومان · `XL` 3+ أيام

---

## القرارات المعتمدة

| # | القرار | الحالة |
|---|---|---|
| 1 | **مسار الرحلة:** الطلب الفوري (`RideRequests` + `Matching`) هو المنتج. `Trips` المجدولة = «خطّي الشهري» بتدفّق منفصل | ✅ |
| 2 | **الهوية:** أزرق `#1259E3` · بلا لون ثانوي · فاتح فقط. و**«مسار» اسم نظام التصميم لا اسم المنتج** — المنتج **رفيق** (`app.json` · `i18n.appName`). هذا السطر كان يقول «الهوية: «مسار»» فقُرِئ كاسم علامة وطُبِع على ٥٨ ملصقاً | ✅ |
| 3 | **الشعار:** «الطريق هو الحرف» · القفلة (أ) علامة + كلمة | ✅ |
| 4 | **Splash:** فاتح | ✅ |
| 5 | **سايدبار الأدمن:** فاتح | ✅ |
| 6 | **الكثافة:** المقياس المكثَّف · لمس 44+ (54 في وضع الرحلة) | ✅ |
| 7 | **الوضع الداكن:** يُحذف | ✅ |
| 8 | **الأيقونات:** Lucide حصراً | ✅ |
| 9 | **السنّ الأدنى: 18 سنة** (سنّ الرشد الأردني) — يُمنع من هم تحتها | ✅ |
| 10 | **الإلغاء بعد الصعود:** بعد `capture` لا إلغاء — «مشكلة في الرحلة» تفتح نزاعاً | ✅ |
| 11 | **نموذج التسعير:** «المقعد الثابت» — مصفوفة (منطقة↔جامعة) بلا surge | ⚠️ **7 أرقام تحتاج حسمك** → [`product/PRICING.md`](product/PRICING.md) §7 |
| 12 | **الشِعار:** «مقعدك إلى الجامعة» · الوعد «تدفع مقعداً، لا رحلة» | ✅ |
| 13 | **نقطة وجهة الشعار كهرمانية** `#F59E0B` — الاستخدام الوحيد المسموح للون ثانٍ | ✅ |
| 14 | **كود الصعود/الإنزال: 4 أرقام** (وهو ما يولّده الكود أصلاً) — و**throttle إلزامي** لا اختياري، لأنّ 4 أرقام = 10,000 احتمال | ✅ القرار · ⏳ الـthrottle بند 1.20 |
| 15 | **السبلاش:** خريطة باهتة + العلامة + الشِعار · فاتح للطالب، داكن للكابتن | ✅ |
| 16 | **بار التنقّل:** مرفوع بظلّ لا حدّ · كبسولة `brand-600` مصمتة وأيقونة مصمتة للنشط | ✅ |
| 17 | **التعرفة بيانات لا كود** — جدول بنسخة وتاريخ إقرار ومرجع قرار، لأنّ الهيئة تُقرّها لا نحن | ✅ القرار |
| 18 | **المضاعف الليلي 1.25 يُحذف** — «تجاوز التعرفة» مخالفة عقوبتها تصل لسحب الرخصة | ✅ القرار · ⏳ بند 5.0 |
| 19 | **هيكل المحفظة: عميل لمرخَّص من البنك المركزي لا دفتر أستاذ داخلي** | ⚠️ **محجوب على سؤال المحامي 3** |

> **⚠️ قرار مُعلَّق يحجب تصميماً:** نموذج التجميع قانونياً أقرب إلى **«خطّ نقل عام منتظم»**
> من «تطبيق نقل ركاب»، والرخصتان مختلفتان. التفصيل والأسئلة التسع في
> [`legal/JORDAN-COMPLIANCE.md`](legal/JORDAN-COMPLIANCE.md) — **الأسئلة 1–4 تحجب تصميماً لا تنفيذاً.**

## القواعد

1. فرع واحد `main` · كل عمل في `feat/`·`fix/`·`chore/`·`docs/` وينتهي بـPR وحذف تلقائي
2. **رقم Issue** هو المعرّف — لا `RFQ-###` جديد (RFQ‑366 الأخير)
3. **CI أخضر إلزامي** — بلا استثناء، بلا `--no-verify`
4. **كل إصلاح مالي أو أمني يحتاج اختباراً يفشل قبله وينجح بعده**
5. لا مرحلة تبدأ قبل إغلاق بوابة التي قبلها
6. الحذف قبل الإضافة

---

# ✅ المرحلة 0 — المنجز

| # | المهمة | النتيجة |
|---|---|---|
| 0.1 | إصلاح CI الأحمر — تعارض `@types/react` (`^18.3.3` مقابل override `18.2.79`) أنشأ نسخة متداخلة `18.3.31`، وأنواع React 19 أضافت `bigint` لـ`ReactNode`. + تحويل CI والصورة إلى `npm ci` | **PR #66 · `main` أخضر** |
| 0.2 | ضمّ `foundation` إلى `main` · حذف **54 فرعاً** (66→9) · `main` صار الافتراضي | ✅ |
| 0.3 | حذف الـzip 6.8MB · `splash-map.jpg` ×2 · `SECURITY_QA_AUDIT.md` · 6 صفائح متجاوزة | ✅ |
| 0.4 | توحيد الوثائق: 20 ملفاً → **5** | ✅ |

---

# ✅ المرحلة 1 — إسعاف مالي وأمني · **مكتملة**

**الحصيلة:** 20 بنداً · **248 اختباراً** (كان 222) · Postgres حقيقي · PHPStan نظيف · مُثبَتات baseline 216 → **211**

| # | المهمة | النتيجة |
|---|---|---|
| 1.1 | فصل الواجبات المالية — `wallet.credit` و`payouts.approve` لأدمن فقط | ✅ PR #71 |
| 1.2 | ضوابط شحن المحفظة — سقف · سبب إلزامي · تدقيق | ✅ PR #71 |
| 1.3 | حرس الأدمن — منع تعديل الذات وإزالة آخر أدمن | ✅ PR #71 |
| 1.4 | تعريف `trips.view` | ✅ PR #71 |
| **1.5** | **المال يتبخّر** — الائتمان كان داخل `if ($captainUser)` بلا `else`، ثم يُكتب `paid_at` فتصبح العملية idempotent فلا تُعاد. **واكتُشف مثله في جانب الطالب:** `if ($student)` بلا `else` كان يُقيّد للكابتن بلا خصم من أحد = **خلق رصيد غير مغطّى** | ✅ يرمي ويُلفّ · 3 اختبارات |
| **1.6** | **انزياح 3 ساعات** — `Core\Support\Clock` يطبّع عند الحدّ. واكتُشف أنّ الأردن **UTC+3 طوال السنة** منذ 27 تشرين الأول 2022، فأي افتراض `+02:00` شتوي خطأ | ✅ 11 اختباراً |
| **1.7** | **`cancelBooking`** كان سطراً واحداً في المتحكّم: يقلب حالة ويترك **الحجز المالي مجمَّداً للأبد**، والطلب مثبَّتاً على رحلة تركها الطالب، ورحلة الاشتراك غير مُعادة | ✅ نُقل للخدمة داخل معاملة بقفل |
| **1.8** | **منع الإلغاء بعد `capture`** + منع الإلغاء المتكرر (كان يعيد تشغيل سجلّ الاحتيال والإشعار ومراقبة الشبح) | ✅ |
| **1.9** | **أقفال `Trips`** — السعة كانت تُفحص **خارج** المعاملة فيأخذ طالبان المقعد الأخير · والصعود يقرأ ثم يكتب بلا قفل فيُحصَّل مرّتين | ✅ + اختبارات قفل حقيقية |
| **1.10** | **`consumeRide` مقفولة** + **مسار احتياطي**: كانت ترمي داخل معاملة الصعود بلا بديل، فراكب انتهى اشتراكه بين الحجز والصعود **لا يستطيع الصعود أصلاً** ولو كان في محفظته رصيد | ✅ + `restoreRide` |
| 1.11 | إيقاف بثّ الموقع بعد الخروج | ✅ PR #71 |
| **1.12** | **توكن الأدمن خرج من `localStorage`** إلى كوكي `httpOnly` عبر وسيط BFF على Next. **صفر مرجع للتوكن في المتصفح** | ✅ |
| **1.13** | **بيئة اختبار حقيقية** — Postgres بدل SQLite `:memory:` + تغطية + `failOnRisky`. **أظهر 5 اختبارات كانت تنجح على SQLite لأنّها بلا أنواع** | ✅ `scripts/pg-test.sh` |
| 1.14 | إغلاق تسريب OTP | ✅ PR #71 |
| 1.15 | تقنيع اللوج · SMS يرمي لا يتدهور | ✅ PR #71 |
| **1.16** | **قفل `PayoutService`** — `approve` كانت **بلا معاملة أصلاً**: مشرفان معاً يعتمدان السحب نفسه فيُدفع للكابتن مرّتين، بسجلَّي تدقيق يقولان إنّ الأمر سليم | ✅ |
| 1.17 | حذف `devLogin` | ✅ PR #71 |
| **1.18** | **`/config` العام كان يسرّب مفتاح الخرائط للسيرفر** — وهو المقيَّد بـIP لا بمُحيل، فأي أحد يقرأه ينفق حصّتنا. فُصل عن مفتاح العميل | ✅ اختباران |
| **1.19** | **العملة بثلاث منازل** — وُجدت في **27 موضعاً** لا 8. دالة واحدة `@rafeeq/shared` + **27 اختباراً** + بوّابة CI. والبوّابة أمسكت موضعاً فاتني (`toFixed(0)` يحذف الفلس كلّه) | ✅ |
| **1.20** | **throttle على الصعود/الإنزال** — 6/دقيقة لكل كابتن **ولكل رحلة**، وكل محاولة فاشلة مُدقَّقة | ✅ |

**🚪 بوابة 1 مغلقة:** لا مال يتحرّك بلا طرفَين · لا حجز يبقى مجمَّداً · لا عملية مالية بلا قفل.

### ما أضافته المرحلة بلا أن يكون في خطتها

| الاكتشاف | كيف ظهر |
|---|---|
| **5 اختبارات كانت تمرّ على SQLite فقط** — تمرّر `'trip-x'` و`'abc'` إلى أعمدة `uuid` | البند 1.13 لحظة تشغيلها على Postgres |
| **`canListen` كانت تنفجر على معرّف مشوَّه** — واسم القناة يأتي من العميل، فأي أحد يستطيع إطلاقها | نفس الاختبارات |
| **الـFK يحمي أقوى من حرس PHP** — `trip_passengers.student_id` يرفض اليتيم أصلاً، فالسيناريو غير قابل للحدوث | كتابة اختبار 1.5 |
| **الأردن UTC+3 طوال السنة** منذ 2022 — لا توقيت صيفي | التحقّق من قاعدة المناطق بدل الاعتماد على الذاكرة |
| **الدين الساكن نزل** 216 → 211 مُثبَّتاً | إعلان خصائص `TripPassenger` أسقط 5 مُثبَتات قديمة |

---
# ✅ المرحلة 2 — الامتثال والقانون · **مكتملة**

**الحصيلة:** **273 اختباراً** (كان 248) · أصول المتجر مولَّدة · حذف حساب حقيقي · بوّابة سنّ 18 · الوثائق القانونية أُعيد كتابتها من الكود

| # | المهمة | النتيجة |
|---|---|---|
| **2.1** | **ترقية Expo SDK** | ⏳ **البند الوحيد المتبقّي** — يحتاج `eas build` باعتمادات Expo. أُضيفت `expo-updates` و`versionCode`/`buildNumber` و`runtimeVersion` |
| **2.2** | **`expo-location` في تطبيق الكابتن** — كان يستوردها **وهي غير مثبَّتة** فالتتبّع معطّل صامتاً وميزات السلامة ساقطة | ✅ مثبَّتة ومُعلَنة · وحُذفت `expo-document-picker` (صفر استخدام) |
| **2.3** | **`apiUrl` إنتاجي** — كان `localhost:8000` مُلتزَماً في `app.json`، فالبناء الإنتاجي يشير إلى loopback الهاتف **ويخالف ATS** | ✅ `resolveApiBaseUrl` **يرمي** على URL ناقص أو `http://` في بناء إصدار — لا افتراضي صامت |
| **2.4** | **أصول المتجر** — أيقونة 1024 · تكيّفية · سبلاش 1284×2778 · إشعار · favicon | ✅ مولَّدة من العلامة. **والحبر كان 39٪ فصار 60٪** لأنّ القياس كان بـ`viewBox` لا بصندوق الحبر |
| **2.5** | **الأذونات** — كانت `locationAlwaysAndWhenInUse` بلا مبرّر | ✅ `WhenInUse` فقط · **الخلفية محجوبة صريحاً** · مبرّرات عربية لكل إذن · `RECORD_AUDIO` و`WRITE_EXTERNAL_STORAGE` محجوبة |
| **2.6** | **حذف الحساب** — كان `$user->delete()` ناعماً يترك الاسم والهاتف والبريد **وكل وثائق الهوية** | ✅ `AccountErasureService` · 9 اختبارات · **والرقم الوطني كان يبقى في `driver_profiles` بعيداً عن متناول حذف صفّ المستخدم** |
| **2.7** | **الخصوصية والشروط** — الطالب يوجّههما لشاشة الدعم، **والكابتن لشاشة المحادثة** | ✅ `LEGAL_URLS` من البيئة · `Linking.openURL` |
| **2.8** | **`privacy-ar.md`** | ✅ من 28 سطراً إلى وثيقة كاملة **مكتوبة من الكود** — كل معالج فرعي مُفصَح (FCM · Maps · WhatsApp · OpenAI · CliQ) · نقل خارج الأردن · حقوقك · ما لا نفعله |
| **2.9** | **`terms-ar.md`** | ✅ من 25 سطراً إلى وثيقة كاملة — **إصدار مُخزَّن** · جدول إلغاء واسترداد · العمولة · التعرفة المُقرَّة · حدود المسؤولية |
| **2.10** | **سنّ 18** | ✅ هجرة `date_of_birth` + `terms_version` + `terms_accepted_at` · **8 اختبارات** بحدود دقيقة (18 بالضبط اليوم يُقبل، وناقص يوم يُرفَض) |
| **2.11** | **الإيصال إلى OpenAI** — كان `temporaryUrl` **رابطاً عاماً موقَّعاً** لإيصال بنكي، صالحاً 10 دقائق، **غير قابل للإبطال** ويظهر في سجلّات الطرف الثالث | ✅ base64 مضمَّن فقط |
| **2.12** | **ربط `/admin/safety/*`** | ✅ المسارات كانت مُسجَّلة فعلاً — أُصلحت الصفحة |
| **2.13** | البيانات الوهمية | ✅ |
| **2.14** | **تقنيع PII** — كل من يملك `users.view` بما فيه **الدعم** يرى كل رقم هاتف في النظام | ✅ `Shared\Support\Pii` · صلاحية `users.view_pii` **منفصلة لا مُضمَّنة** · 9 اختبارات · القائمة مقنَّعة أيضاً |
| **2.15** | **تسريبات الإشعارات** | ✅ لا اسم مُرسِل ولا نصّ محادثة · **ولا اسم ضحية طوارئ** — كان يُذاع على كل الموظّفين ويتدهور إلى SMS |
| **2.16** | قرار هيكل المحفظة | ✅ موثَّق في `JORDAN-COMPLIANCE.md` §5 — عميل لمرخَّص |
| **2.17** | ملفّ أسئلة المحامي | ✅ التسعة موثَّقة · **وقرارك: نمضي باعتبار كل شيء قانونياً** |
| **2.18–2.26** | تكامل LTRC · سجلّ الفحص اليومي · المركبة المصرَّحة · مراجعة الكوبونات · DPO · تقريب الموقع | ⏳ **تكامل LTRC يحتاج API من الهيئة غير منشور** · البقية في المرحلة 3 |

**🚪 بوابة 2:** حذف الحساب يعمل ويُجهِّل فعلاً · سنّ 18 مفروض ومُختبَر · كل معالج فرعي مُفصَح · صفر PII في إشعار · صفر `localhost` في بناء إنتاجي.

### ما أضافته المرحلة بلا أن يكون في خطتها

| الاكتشاف | التفصيل |
|---|---|
| **الخطّ لم يكن مثبَّتاً أصلاً** | كل صفائح التصميم كانت تتراجع صامتة إلى **Noto** لأنّ `IBM Plex Sans Arabic` غير موجود على الجهاز. صار مُحزَّماً في `design/src/fonts/` بثلاثة أوزان |
| **الكابتن يوجّه الخصوصية لشاشة المحادثة** | التدقيق قال «مكسور»، والواقع أسوأ: يفتح شاشة دردشة |
| **`app.json` كان يحمل الهوية الملغاة** | `#002045` كحلي و`#006A65` تيركواز في السبلاش والأيقونة والإشعارات |
| **الرقم الوطني بمنأى عن حذف الحساب** | في `driver_profiles` لا `users`، فالحذف الناعم لا يصله |
| **الحبر 39٪ من الأيقونة** | القياس كان بـ`viewBox` والعلامة قُطرية |
| **مسار الخدمة كسر نفسه** | `erase()` يحذف ناعماً، فالمحاولة الثانية تفشل بـ«غير موجود» بدل «محذوف بالفعل» — أُصلح بـ`withTrashed` |

---
# ✅ إضافات بعد المرحلة 2 — **مكتملة**

| # | العمل | النتيجة |
|---|---|---|
| **أ** | **الدفع النقدي للكابتن** | ✅ **20 اختباراً** · 273 → 293 |
| **ب** | **جرد كل شاشة في المشروع** | ✅ [`design/SCREENS.md`](design/SCREENS.md) — **77 شاشة** مستخرَجة من شجرة الملفّات لا من الذاكرة |
| **ج** | **ترتيب الوثائق** | ✅ 8 ملفات مسطّحة → `engineering/` · `product/` · `design/` · `legal/` + 3 في الجذر |
| **د** | **مجلّد تسويق بمخرَجات جاهزة** | ✅ [`../marketing/`](../marketing/) — **26 صورة + 20 نصّاً** |

### الدفع النقدي — ولماذا لم يكن «تخطّي الخصم»

النقد **يعكس اتجاه المال**: على المحفظة تحمل المنصّة الأجرة وتدفع للكابتن حصّته فلا تتعرّض
لشيء؛ وعلى النقد **يحمل الكابتن الأجرة كاملة ويصير مديناً بالعمولة** — فتصبح المنصّة **دائنةً
لكل كابتن، باستمرار**. وهذا تعرُّض حقيقي لا تفصيل محاسبي:

| القرار | السبب |
|---|---|
| **الأجرة تُسجَّل بسعر النطاق المعلن** | الكابتن يقبض الورق **لا يحدّد السعر** — والقيد مطابق للمحفظة تماماً، فتبقى التعرفة قابلة للمراجعة |
| **تُخصَم من الرصيد أولاً** | من له أرباح من رحلات بالمحفظة **لا يتحمّل شيئاً إضافياً** — وهي الحالة الشائعة |
| **الفارق ديناً موجباً لا رصيداً سالباً** | فيبقى `balance_fils` يعني شيئاً واحداً، و`availableFils()` صحيحة بلا استثناءات |
| **السقف يمنع الاتصال لا الرحلة الجارية** | قطع رحلة لتحصيل دَين **لا يُدافَع عنه** — والأجرة المُحصَّلة هي ما يسوّيه أصلاً |
| **الأرباح تُسوّي الدَين تلقائياً** | فلا يُطلَب من الكابتن أن يتذكّر |
| **كل حركة دَين لها قيد تدقيق** | دَين لا يُدقَّق = نزاع مؤجَّل |

**وعُدِّلت الشروط:** كانت تمنع «التحصيل النقدي خارج المنصّة» — وهذا صحيح ويبقى. لكن النقد
**داخل** المنصّة بالسعر المعلن شيء آخر، والفرق بينهما هو التصميم كلّه، فصار منصوصاً.

---

# ✅ المرحلة 3 — الخصوصية وسلامة الدفتر — **مكتملة**

**الحصيلة:** **352 اختباراً** (كان 293) · 12 سياسة احتفاظ منفَّذة بأمر واحد · 24 قيد `CHECK`
على المال · 7 مفاتيح مالية إلى `RESTRICT` · 9 مفاتيح أجنبية جديدة · **كل بيانات التعريف
مشفَّرة على القرص** · نسخة احتياطية تتحقّق بالاستعادة

| # | المهمة | النتيجة |
|---|---|---|
| **3.1** | **الاحتساب المزدوج** — العمولة على مقعد يغطّيه اشتراك كانت تُحتسب إيراداً، **ونفس المال محتسَب مرّة ثانية** كبيع اشتراك | ✅ التقرير صار مجموعتين لا تُجمعان: **قيمة التعرفة** (تُغلق: `gross = commission + captain_share + discount`) و**النقد الفعلي** (`platform_revenue_fils`). **والكوبون لم يكن يُجمَع أصلاً** فكان التقرير لا يتوازن. **و`by_zone` كان يتجاهل فلتر المنطقة** فنصفا الشاشة يتناقضان |
| **3.2** | `restrictOnDelete` على الجداول المالية | ✅ 7 مفاتيح · صفر `CASCADE` مالي · 9 اختبارات |
| **3.3** | `CHECK (x >= 0)` على أعمدة المال | ✅ **24 عموداً** — Postgres بلا `unsigned` وLaravel يُسقط الكلمة صامتاً، فكلّها كانت تقبل السالب. `wallet_transactions.amount_fils` **مستثنى بقصد** (موقَّع بالتصميم، موثَّق بـ`COMMENT ON COLUMN`) |
| **3.4** | تقليم `driver_locations` + الرحلات العالقة | ✅ **و`driver_locations` لم يكن له تقليم إطلاقاً** · والأمر القديم كان يفلتر `Completed\|Cancelled` فرحلة تُركت في `started` تحفظ مسارها **للأبد** |
| **3.5** | حذف وثائق الكابتن المرفوضة | ✅ **الملفّ من القرص قبل الصفّ** |
| **3.6** | تقليم التذاكر والمدفوعات | ✅ |
| **3.7** | `ExpireSubscriptions` + فهرس + `endOfDay` | ✅ 8 اختبارات · فهرس `(status, ends_at)` · **خطّة تنتهي «حتى الثلاثين» تبقى فعّالة كلّ الثلاثين** |
| **3.8** | **تشفير الهاتف/الاسم/العناوين + فهرس للبحث** | ✅ **17 اختباراً** — الاسم والهاتف والبريد والرقم الوطني والعنوان المكتوب **وجهات اتصال الطوارئ**. `BlindIndex` (HMAC‑SHA256) يعيد الدخول والتفرّد والبحث |
| **3.9** | نسخ احتياطي + اختبار استعادة | ✅ `backup.sh` **يتحقّق بالاستعادة** إلى قاعدة مؤقّتة ومقارنة عدد الصفوف — dump لا يُستعاد ملفّ لا نسخة |
| **3.10** | healthcheck حقيقي + تنبيه `failed_jobs` + `failed()` | ✅ `\|\| exit 0` حُذف · `rafeeq:worker-alive` يُثبت Redis · نبضة للـscheduler · `failed()` على الـjobs |
| **3.11** | مفاتيح أجنبية للمراجع المنطقية | ✅ 9 مفاتيح · **و`trips.zone_id` كان مفتاح تجميع التقرير المالي** فمنطقة محذوفة تُنتج صفّ إيراد لمنطقة لا وجود لها |
| **3.12** | حدّ/تقسيم على المطابقة + chunking | ✅ 7 اختبارات · **والحلّ الواضح (`chunkById`) كان خاطئاً** ⬇ |
| **3.13** | سياسة لكل جدول ينمو | ✅ 12 سياسة · **و`rafeeq:retention-report` اكتشف 3 جداول بلا سياسة** لم يرها 5 تدقيقات |

**🚪 بوابة 3:** ✅ 6/6 التزامات منفَّذة (**كان 2/6**) · ✅ استعادة مُختبَرة فعلاً · ✅ صفر جدول
بلا سقف · ✅ الهوية المالية تُغلق على كل أنواع المقاعد · ✅ صفر بيان تعريف مقروء على القرص.

### ما أضافته المرحلة بلا أن يكون في خطتها

| الاكتشاف | التفصيل |
|---|---|
| **وثيقة الاحتفاظ كانت تكذب في اتجاهين** | وعدت **12 شهراً** لرمز دخول يُحذف بعد **يوم**، و**«لا يُحفَظ»** لموقع كابتن كان يُحفَظ **بلا سقف**. صارت [`legal/data-retention-ar.md`](legal/data-retention-ar.md) **مولَّدة من الكود** بأمر واحد |
| **`RAFEEQ_TRACKING_RETENTION_DAYS` حُذف** | متغيّر بيئة كان يتجاوز صامتاً مدّة **منشورة في وثيقة خصوصية**. وعد يمكن تغييره بلا مراجعة كود ولا اختبار ولا تحديث وثيقة ليس وعداً |
| **`RetentionPolicy` كانت تسمّي أوامر غير موجودة** | 7 من 12 سياسة تشير إلى `rafeeq:prune-*` لا وجود له. الآن اختبار يؤكّد أنّ كل سياسة تشير لجدول وعمود **موجودين فعلاً** |
| **العمولة المضخَّمة تسرّبت إلى الملخّص الذكي** | `AdminInsightsService` كان يمرّر الرقم المزدوج إلى GPT وإلى السرد العربي بعبارة «إيراد عمولة» — فالمدير يُبلَّغ برقم أعلى من الحقيقة |
| **`chunkById` على المطابقة كان سيكسر التجميع** | تقسيم الطابور المسطّح **يفرّق ركّاب السيّارة نفسها** على حدود الدُفعات، فيُنتج سيّارات نصف فارغة **تحت الحمل فقط** — خطأ صحّة يظهر عند الضغط لا في اختبار صغير. فانقلب الاستعلام: تُسأل **الممرّات** أولاً، ثم يُستنزف كل ممرّ وحده |
| **`University::find()` داخل حلقة تكوين السيّارات** | 2,500 استعلام متطابق لكل 10,000 راكب. الجامعة خصيصة الممرّ، فصارت تُحلّ مرّة واحدة له |
| **`alertSafetyTeam` كانت مكرّرة ثلاث مرّات حرفياً** | في `Sos` و`Complaints` و`Disputes` — بنفس `->get()` غير المحدود، **داخل طلب زرّ الطوارئ نفسه**، ومع تدهور إلى SMS لكل موظّف. أخطر مسار في المنتج كان أقلّها انضباطاً |
| **الرقم الوطني لم يكن قابلاً للمقارنة** | كان مشفَّراً، فلا أحد يعرف أنّ كابتنين قدّما **نفس الرقم**. كابتن موقوف يعود برقم هاتف جديد ونفس الهوية. الآن فهرس فريد على البصمة |
| **`unique:users,phone` كانت ستنجح دائماً** | القاعدة تُترجَم إلى `where phone = ?` وعلى نصّ مشفَّر عدّها صفر أبداً — فتمرّ ثم يموت الطلب على فهرس القاعدة بـ500 بدل رسالة «الرقم مستخدم». اختبار تحقّق **ينجح دائماً** أسوأ من غيابه |
| **`select()` المضيَّق كان سيمحو فهارس البحث** | نموذج محمَّل بـ`select(['id','phone'])` لا يملك `full_name` إطلاقاً، فحساب البصمات بلا حراسة كان سيقرأ `null` **ويمحو أسماء البحث** عند أي حفظ لاحق — ومستخدم يصير غير قابل للعثور لسبب لا يربطه أحد بشاشة سحب أرباح |

### التنازل المُعلَن في 3.8

البحث بالأجزاء **انتهى**. الدعم يبحث بمطابقة تامّة: رقم كامل (بأي صيغة)، بريد كامل، أو
**كلمة كاملة** من الاسم (مع طيّ الهمزات والتاء المربوطة والتطويل، فـ«احمد» تجد «أحمد»).
البديل — فهرس n‑gram — يُفشي ما يكفي لإعادة بناء الأسماء التي جاء ليحميها، والبديل الآخر
فكّ تشفير الجدول عند كل حرف. **والاختبار يثبّت التنازل صراحة** حتى لا «يُصلحه» أحد لاحقاً
دون أن يدرك ما يفعله.

---

# ✂️ المرحلة 4 — الحذف الكبير · 5 أيام

| # | المهمة | الحجم |
|---|---|---|
| **4.1** | احذف `Modules/Areas` + `Modules/PickupPoints` (~1336 سطر) | S |
| **4.2** | احذف 7 شاشات ميتة (791 سطر) | S |
| **4.3** | احذف 38 دالة api-client بلا مستدعي + `ServiceTile` + `POST /wallet/topup-instructions` + `AdBanner` من الرئيسية | M |
| **4.4** | احذف اللوحة الداكنة + 57 `dark:` + الأسماء المستعارة `gold/cyan/blue/navy` + تعليقات الهويات الميتة | M |
| **4.5** | **أكمل `support.show`/`reply`** ثم ادمج `Complaints` في `Support` | L |
| **4.6** | صِل `PATCH/DELETE /driver/vehicles/{id}` و`ratings.mine/received` | M |
| **4.7** | أجّل `Exchange`/`Parcels`/`LostFound` إلى `parking/marketplace` | S |
| **4.8** | سكربت CI: يفشل عند أي شاشة بلا مسار وصول أو دالة API بلا مستدعي | M |

**🚪 بوابة 4:** 20 موديولاً · صفر شاشة ميتة · صفر دالة بلا مستدعي.

---

# 💰 المرحلة 5 — محرك التسعير · 5 أيام

**✅ رُفع الحجب — السبعة محسومة في [`product/PRICING.md`](product/PRICING.md) §7**

> **وواحد منها كان بلاغاً خاطئاً:** البند 5.5 ادّعى تناقضاً في العمولة بين `config` (15%)
> ولوحة الإدارة (12%). لا يوجد 12% في الكود — الرقم بيانات وهمية في صفائح التصميم
> (`gen-admin.mjs` قيد تدقيق نموذجي، و`gen-driver.mjs` نسبة تغيّر يومي). تدقيق قرأ
> لقطة تصميم كأنّها إعداد.

> **واكتُشفت ثغرة مراجحة لم تكن في الخطّة:** سعر اشتراك مسطَّح (48 د.أ) على كل
> النطاقات يجعل طالب النطاق A يدفع **أكثر** من السعر العادي وطالب النطاق F يوفّر 47% —
> اختيار عكسي يضمن أن كل مشترك هو الأكثر كلفة. السعر صار يُشتقّ من نطاق الخطّ.

| # | المهمة | حجم | الحالة |
|---|---|---|---|
| **5.1** | أعِد كتابة `PricingService` — احذف surge والتعريفة الليلية والتسعير بالدقيقة · نطاقات ثابتة · معامل المنفرد | L | ✅ |
| **5.2** | نافذة تجميع في `MatchingService` + حدّ أدنى للامتلاء قبل الإرسال | L | ✅ |
| **5.3** | ضمان الحدّ الأدنى للكابتن بسقف يومي — يُدفع من العمولة لا من الطالب | M | ✅ |
| **5.4** | مصفوفة (منطقة↔جامعة) كمصدر السعر الوحيد + نطاق ومنشأ لكل صف | M | ✅ |
| **5.5** | ~~حسم تناقض العمولة~~ — **بلاغ خاطئ، لا تناقض**. صار محلّه: **محفظة المنصّة** (اكتُشفت أثناء التنفيذ) | S→L | ✅ |
| **5.6** | أصلح تسرّب التقريب (`intdiv`) ودقّة الفلس (`toFixed(2)` تبتر خانة) | S | ✅ |
| **5.7** | اختبارات: كل نطاق · المنفرد · الضمان بسقوفه الثلاثة · zero-sum · التقريب | M | ✅ |

**🚪 بوابة 5:** ✅ المقعد النموذجي 1.500 < 2.000 · المنفرد = المقاعد كلها · **صفر surge** · **اختبار zero-sum أخضر** · 405 → 421 اختباراً · PHPStan صفر خطأ.

### ما لم يكن في الخطّة واكتُشف أثناء التنفيذ

> **١. العمولة لم تكن تُقيَّد في أي حساب — والدفتر لم يكن متوازناً أصلاً.**
> تابِع مقعد النطاق C في الدفتر: الطالب يُخصم منه 1500، والكابتن يُقيَّد له 1275،
> و**225 فلساً لا تُكتب في أي مكان**. لم تكن العمولة تُنقل ولا تُحفظ — كانت الفرق
> الحسابي بين قيدين، وتخرج من الدفاتر كلّياً. نتيجتان:
> **(أ)** إيراد المنصّة غير قابل للإجابة من الدفتر — «كم ربحنا الشهر الماضي؟» يتطلّب
> جمع `trip_passengers.commission_fils`، وهو عمود يكتبه نفس المسار الذي تريد التحقّق منه.
> **(ب)** الضمان (5.3) لا مصدر تمويل له، فـ`credit(captain, shortfall)` **يخلق رصيداً
> بلا غطاء** — وهو بالضبط العيب الذي يحرسه `RideBillingService` في الكوبونات بسقف
> «لا توجد محفظة خزينة مموَّلة». فأُنشئت تلك المحفظة: `wallets.kind='platform'`، تُقيَّد
> فيها العمولة وتُخصم منها الإعانات. ولأنّ `WalletService::apply()` يرفض أي رصيد سالب،
> **الخزينة لا يمكن كشفها** — أي أنّ المنصّة لا تستطيع دعم أكثر مما جنته فعلاً، وتحذير
> «الدعم وحده يُفلس المنصّة» صار مفروضاً بنفس الكود الذي يمنع طالباً من دفع ما لا يملك،
> لا بثلاث سياسات وأمل في ضبط إعداداتها.
>
> **٢. حذف التسعير بالمسافة تسرّب من `/estimate` إلى المُطابِق.**
> `/estimate` صار يرفض التسعير على مسار بلا سعر معتمد ويجيب `in_coverage:false`. لكن
> **إنشاء** الطلب كان يفحص التغطية الجغرافية فقط، فيُقال للطالب «ما وصلنا لمنطقتك» ثم
> يُقبل طلبه، فيُسعّره المُطابِق من `default_fare_fils`. أي أنّ «السعر الذي لم يعتمده
> أحد» لم يُحذف — انتقل إلى مكان لا يبحث فيه أحد. الرفض صار في المدخلين.
>
> **٣. شاشة التسعير في الأدمن كانت تعرض 13 مقبضاً، تسعة منها لا تفعل شيئاً.**
> `per_km_fils`·`per_min_fils`·`base_fare_fils`·`min_fare_fils`·`avg_speed_kmh`·
> `night_multiplier`·`night_start_hour`·`max_surge_multiplier` — حقول تُحفظ ويرفضها
> الـAPI بصمت. مقبض يتحرّك ولا يحرّك شيئاً أسوأ من غياب المقبض: المدير سيضبطه ويبني
> عليه هامش شهر.
>
> **٤. تطبيق الطالب كان سيعرض ثلاث بطاقات سعر فارغة.**
> `estimateClasses()` كان ينادي `/estimate` **بلا إحداثيات ولا جامعة** — وهو ما كان
> يعمل فقط لأنّ الـAPI كان يخترع رقماً من المسافة. بعد حذف ذلك صار الجواب
> `unpriced_corridor` بلا `fare_fils`، فتُقرأ `undefined` وتُطبع «—» دائماً. صار
> `FareQuote` نوعاً اتّحادياً (`zone_matrix` | `unpriced_corridor`) ليُجبر المصرِّف
> المستدعي على معالجة الحالتين، بدل حقول اختيارية تفشل بصمت.

---

# 🎨 المرحلة 6 — التوكنز · **مكتملة**

**المصدر صار واحداً: `frontend/packages/tokens`.** كانت القيمة اللونية الواحدة مكتوبة
بيد في **أربعة** أماكن، وثلاثة منها انحرفت — وما مِن شيء كان يقارنها، فما مِن شيء اكتشف
ذلك. الآن `kit.css` و`tailwind.config.ts` **مولَّدان**، و`shared/src/theme/**` محذوف،
وبوابة CI تُعيد التوليد وتفشل على أي فرق.

| # | المهمة | حجم | الحالة |
|---|---|---|---|
| **6.1** | `packages/tokens` (10 ملفات · 1,497 سطراً) → يولّد `kit.css` + Tailwind preset · `check:tokens` يرصد الانحراف | L | ✅ |
| **6.2** | حذف `packages/shared/src/theme/**` (5 ملفات) · `theme.ts` في التطبيقين صار سطرين | M | ✅ |
| **6.3** | حذف الوزن الوهمي `extrabold`: **91** موضعاً في التطبيقين + **31** صنف `font-extrabold` في الأدمن | S | ✅ |
| **6.4** | Lucide في الثلاثة: سجلّ مولَّد بـ**99** اسماً على **93** مكوّناً · **45** اسماً في الأدمن · Feather وMaterial Symbols محذوفان بالكامل | M | ✅ |
| **6.5** | العملة صارت **ثلاث دوال بمدخل واحد** (فلس): `formatJod` · `formatJodSigned` · `bareJod` — **27** ملفاً · الأربع الديناريّة **محذوفة لا مُهملة** | M | ✅ |
| **6.6** | عزل ثنائي الاتجاه: `num.ts` + `<Num>` في التطبيقات الثلاثة | M | ✅ |
| **6.7** | **8 بوابات** في `check-design-tokens.mjs` + `check:tokens` + `check:icons` + `check:money`، كلّها في CI | L | ✅ |

**🚪 بوابة 6:** ✅ الهوية المتقاعدة **0** · الوزن الوهمي **0** · `dark:` **0** · تباين <4.5 **0** ·
`check:tokens` بلا انحراف · `check:icons` 45/45 اسماً موجوداً · `check:money` نظيف ·
**20** اختباراً جديداً في `packages/tokens` (48 واجهة إجمالاً) · 443 اختبار خلفية · PHPStan صفر خطأ · `npm ci` نظيفة · بناء الأدمن أخضر.

### أرقام الخطّة كانت قديمة — هذه المقيسة

| البند | الخطّة قالت | القياس الفعلي | بعد التنفيذ |
|---|---|---|---|
| hex يدوي | 117 | **140** | **74** (ميزانية) |
| `dark:` | 57 | **0** — كانت محذوفة أصلاً | 0 (صفر صلب) |
| اتجاه فيزيائي (RTL) | 388 | **10** — الـ388 عدّت `left:`/`right:` مشروعة في تحديد مطلق | 10 (ميزانية) |
| `extrabold` | «40+» | **122** | 0 (صفر صلب) |
| `fontSize:` خام | 7 | **457** على 21 قيمة | **428** (ميزانية) |
| صيغ العملة | 8 | 7 دوال متقاطعة | 3 |
| أقطار الحدود | — | 7 قيم على 49 ملفاً | 4 أدوار |

### ثلاثة انحرافات عن الخطّة، مقصودة

> **١. اتّجاه التوليد معكوس: TypeScript هو المصدر، و`kit.css` مولَّد.**
> الخطّة قالت «`packages/tokens` **من** `kit.css`». رُفض لسببين: `kit.css` لا يحمل سلّم
> المسافات ولا سلّم الطباعة ولا الـz-index كخصائص مخصّصة أصلاً — هي مبثوثة داخل قواعد
> الأصناف، فلا يمكن قراءتها آلياً؛ وثلاثة من المستهلكين الأربعة TypeScript. بالاتجاه
> المعكوس تُحذف النسخ اليدوية من **أربعة** أماكن لا ثلاثة، و`kit.css` صار له كتلة
> `@generated:tokens` بين علامتين، فتعديل يدوي فيها تفشله البوابة بدل أن يصبح حقيقة خامسة.

> **٢. أربعة أوزان، لا ثلاثة.**
> الخطّة قالت «3 أوزان فقط». حُذف الوزن **الوهمي** وحده: `extrabold` كان مُسنداً إلى وجه
> الـ700 — فـIBM Plex Sans Arabic لا يشحن وزن 800 — أي أنّ الكود كان يدّعي تراتُباً لا
> تستطيع الشاشة إظهاره. أمّا `semibold` **فأُبقي**: وجهه الحقيقي مشحون في
> `@expo-google-fonts` ويُستعمل في 32 عنواناً مشروعاً. العيب الفعلي لم يكن وجود الوزن — بل
> أنّ `docs/design/src/fonts/` كان يفتقد وجه الـ600، فمصيِّر الصفائح **لم يكن يستطيع
> إظهار عنوان تطبيق كما هو**. أُضيف الخطّ، ولم يُحذف الوزن.

> **٣. البوابات مسنّنات، لا أصفار.**
> صفران صلبان فقط (الهوية المتقاعدة · الوزن الوهمي). الباقي ميزانية **لا تزيد**: أن
> نُعلن 428 حجم خط خاماً صفراً اليوم يعني بوابة حمراء على الفور — والبوابة التي تُثبَّت
> على صفر قبل أن تصل إليه يُعلّقها أوّل من يستعجل. الشاشات التي تحمل هذه الأرقام تُعاد
> كتابتها في المرحلتين 8 و9، فالميزانية تُسدَّد هناك.

### ما لم يكن في الخطّة واكتُشف أثناء التنفيذ

> **١. التحقّق من الأنواع لا يكتشف فشل الحِزم.**
> إعادة تصدير سجلّ الأيقونات المولَّد من حزمة `@rafeeq/tokens` نجحت في **ستّ** عمليات
> `typecheck` — ثم أسقطت `next build`: السجلّ يحمل مكوّنات `lucide-react-native`، فسحب
> `react-native` إلى حزمة الأدمن وارتطم webpack بصياغة Flow داخل `react-native/index.js`.
> لوحة الإدارة كانت تستورد ثوابت تصميم فتحصل على مصيِّر الموبايل كاملاً. لذلك صار السجلّ
> يُستورد بمسار عميق من التطبيقين فقط، **وبناء الأدمن صار خطوة في CI** — فهذا صنف من
> العيوب لا يجده إلا بناء حقيقي.

> **٢. مصدر التصميم لم يكن قابلاً للتشغيل من مكانه.**
> كل مولّد في `docs/design/src` كان يحسب مخرَجه بـ`resolve(HERE, '../Rafeeq-JO/...')` — مسار
> لا ينحلّ إلا إذا نُسخ السكربت بجانب نسخة من المستودع. مُشغَّلاً من مكانه المُودَع، كما
> يوصي `docs/design/README.md` حرفياً، كان يُنشئ شجرة وهمية `src/Rafeeq-JO/` ويترك الصفائح
> الحقيقية كما هي — **فإعادة توليد تبدو ناجحة ولا تُغيّر شيئاً**. وفي `gen-posters.mjs`
> كان مسار متصفّح خاصّ بصندوق تطوير **مُودَعاً في المستودع**. صُحّح الكل، وأُعيد توليد
> `docs/design/v2` و`docs/design/readme` من مكانها.

> **٣. Chromium لا يصوّر نافذة أصغر من ~50×50 — ولا يُخطئ، بل يتجمّد.**
> `gen-app-assets.mjs` (favicon 48×48) و`gen-icons.mjs` (48×48) كانا يموتان على مهلة
> Playwright بعد أن يقول «fonts loaded» — رسالة تقرأ كمشكلة خطوط في صفحة لا نصّ فيها،
> وعلى أصل مختلف كل مرّة فبدت رعشة لا حدّاً. القياس على Chromium 1232 لنفس الصفحة:
> 48×48 تنتهي مهلته، و96×96 يصوّر في 12 مللي ثانية. صار التخطيط في نافذة 128 والقصّ إلى
> المقاس الحقيقي. ومثله في `shoot-readme.mjs`: نافذة 80px مع `fullPage` كانت تتجمّد،
> فصار القياس ثم التحجيم ثم التصوير — **وكل الأبعاد الناتجة مطابقة تماماً للمُودَع**.

> **٤. `gen-marketing.mjs` كان كوداً ميتاً يُفسد أصلاً منشوراً.**
> `marketing/README.md` يوثّق `gen-posters.mjs` مولّداً وحيداً للملصقات الـ62. أمّا
> `gen-marketing.mjs` (511 سطراً، من PR #79) فبقي بعد أن حلّ محلّه: تشغيله يكتب 11 ملفاً
> يتيماً **ويستبدل `02-monthly-math.png` المشحون بتصميم أقدم**. حُذف.

> **٥. `npm run lint` في جذر الفرونت لم يكن يعمل إطلاقاً.**
> `eslint . --ext .ts,.tsx` بلا أي ملف إعداد في الجذر — يخرج بخطأ «couldn't find a
> configuration file». صار يفوّض إلى مساحة العمل الوحيدة التي لها إعداد. وأنّ التطبيقين
> غير مفحوصين بـESLint **ثغرة قائمة** سُجّلت للمرحلة 7 مع `packages/ui`.

---

# 🧩 المرحلة 7 — مكتبة المكوّنات · 5 أيام

| # | المهمة | حجم |
|---|---|---|
| **7.1** | `packages/ui` + نقل `LiveMap` **مرة واحدة** (510 سطر ×2) | L |
| **7.2** | نقل المتطابقات (`Feedback` 233 · `MapBackdrop` · `Loader` · `Screen` · `Icon`) | M |
| **7.3** | وحّد 9 ملفات منحرفة + `lib/api` + `store/auth` + `i18n` | L |
| **7.4** | `Text` هو المكوّن الوحيد للنص + ESLint يمنع `<Text>` الخام · `Button` 6→4 variants | M |
| **7.5** | **`accessibilityLabel` إلزامية** على كل زر أيقونة (اليوم 0/154) + `<th scope>` في الأدمن | L |
| **7.6** | `KeyboardAvoidingView` في كل شاشة بحقول (ناقصة في 6) | S |
| **7.7** | حالات `loading`·`empty`·`error`·`offline` لكل قائمة + L0–L3 (حدّ أو ظل) | L |
| **7.8** | معالجة مركزية لـ403/422/500 + timeout منفصل للرفع + retry | M |
| **7.9** | احترام `reduce-motion` + إصلاح `useFocusEffect`+`useEffect` المزدوج + سباق `refreshDriver` | M |
| **7.10** | Vitest للمكوّنات الأساسية (اليوم **صفر اختبار فرونت**) | L |

**🚪 بوابة 7:** صفر ملف مكوّن مكرّر · 154/154 زر له تسمية · ≥40 اختبار فرونت.

---

# 📱 المرحلة 8 — تطبيق الطالب · 5 أيام
**19 → 12 شاشة · 4 تبويبات** — المرجع: `docs/design/v2/02·03·04`

| # | المهمة | حجم |
|---|---|---|
| 8.1 | الشل + التبويبات + AI يخرج من المركز إلى حبّة على الرئيسية | M |
| 8.2 | الرئيسية: خريطة + شيت واحد بثلاث نقاط توقّف | L |
| 8.3 | **«مشتركة» مقابل «منفردة»** بسعرين معلنين (بدل فئات المركبة) | L |
| 8.4 | الرحلة الحيّة + التتبّع + رمز الصعود 6 أرقام | L |
| 8.5 | المحفظة + شحن CliQ + **إكمال نوع `Wallet` بحقول التوفّر** | M |
| 8.6 | حسابي + حذف الحساب + الروابط القانونية | M |
| 8.7 | `emergency` يُبسّط من 340 سطراً · `FlashList` في كل قائمة | M |

# 🚗 المرحلة 9 — تطبيق الكابتن · 5 أيام
**14 → 8 شاشات** — المرجع: `docs/design/v2/05`

| # | المهمة | حجم |
|---|---|---|
| 9.1 | يومي + مفتاح متصل بعرض الشاشة (60) + رقم بطل واحد | M |
| 9.2 | العرض الوارد ملء الشاشة + عدّاد + صوت + اهتزاز | L |
| 9.3 | وضع الرحلة: خطوة واحدة · أهداف 54 · **بلا تمرير** · كود 6 أرقام + throttle | L |
| 9.4 | دمج 4 شاشات مالية في واحدة + **«الرصيد المتاح» يعرض المتاح فعلاً** | M |
| 9.5 | مركبتي ووثائقي + تعديل/حذف المركبة | M |

# 🖥 المرحلة 10 — لوحة الإدارة · 5 أيام
**28 → 18 صفحة · 6 → 4 مجموعات** — المرجع: `docs/design/v2/06-admin-1/2/3`

| # | المهمة | حجم |
|---|---|---|
| 10.1 | الشل: سايدبار فاتح + command palette واحدة (حذف الملاحة المزدوجة) | M |
| 10.2 | الصفحات الأربع الحاكمة: لوحة القيادة · الطلبات الحيّة · الكباتن · المدفوعات | L |
| 10.3 | السلامة و SOS · الدعم المدموج · التسعير · التدقيق | L |
| 10.4 | رأس جدول فاتح · `tabular-nums` · `ms/me/start/end` · **سقف `per_page`=100** · 3 حالات لكل جدول | M |
| 10.5 | **i18n حقيقي** (اليوم عربي مثبّت بالكامل) | L |

---

# 🛡 المرحلة 11 — التصليب · 6 أيام

| # | المهمة | حجم |
|---|---|---|
| 11.1 | `throttle:auth` مطبّع + مفتاح لكل endpoint + قفل حساب + backoff | M |
| 11.2 | توكن 7 أيام + refresh + `abilities` مقيّدة بالدور | L |
| 11.3 | **`Policy` + `scopeBindings()`** — اليوم **صفر Policy** والحماية يدوية | XL |
| 11.4 | CORS: `localhost` في `local`/`testing` فقط · أسقط `supports_credentials` | S |
| 11.5 | **reverse proxy + TLS** — كل المنافذ اليوم HTTP خام | L |
| 11.6 | `ENTRYPOINT`/`CMD` في `backend/Dockerfile` (الهجرات لا تُشغَّل اليوم) + `.dockerignore` يستثني `tests/`·`*.md` | S |
| 11.7 | حدود موارد على كل خدمة + healthcheck لـ`reverb` و`admin` | S |
| 11.8 | اختبارات: 5 موديولات صفر تغطية · **اختبار تزامن لكل مسار مالي** · عقود API/i18n بلا `skip` صامت | XL |
| 11.9 | E2E (Maestro): تسجيل→OTP→رئيسية · طلب→دفع · كابتن: قبول→بدء→إنهاء | L |
| 11.10 | لقطات بصرية لكل شاشة + اختبار تكبير خط 200% | M |

**🚪 بوابة 11:** TLS على كل نقطة عامة · `Policy` لكل موديول · 3 مسارات E2E خضراء · تغطية ≥60%.

---

# 🤖 المرحلة 11.5 — الذكاء الاصطناعي: تصليب ثم توسيع · 6 أيام

**الحال:** الذكاء الاصطناعي **موجود ويعمل** — ليست مرحلة بناء من الصفر. سبع نقاط
تماسّ حيّة اليوم: مساعد المحادثة بأدواته الستّ، اقتراحات الرحلات، تصنيف التذاكر،
تصنيف الشكاوى، ترتيب المفقودات دلاليّاً، التحقّق البصري من إيصالات CliQ، وسرد
مؤشّرات الأدمن ومخاطر الحسابات. المطلوب ليس إضافة مزايا — بل **جعل الموجود آمناً
ومحسوباً**، ثم التوسّع.

## أولاً: التصليب — لأنّ ما هو غير محسوب لا يُوسّع

| # | المهمة | حجم | الخطر إن أُهمل |
|---|---|---|---|
| **A.1** | ✅ **`throttle:sensitive` على كل مسار مدفوع** — كان `assistant/send` المسار المدفوع الوحيد بلا حدّ | S | **حساب واحد يقود مئات الاستدعاءات المدفوعة في الدقيقة** |
| **A.2** | **قيد لكل استدعاء مدفوع في دفتر واحد** — اليوم `ai_messages.tokens` يسجّل المساعد فقط؛ التصنيف والرؤية والترتيب والسرد **لا تُسجَّل إطلاقاً** | L | الكلفة الحقيقية غير معروفة وغير محدودة |
| **A.3** | **سقف عام + قاطع تيّار.** `max_monthly_tokens` (50 مليون) **إعداد ميت بلا مستدعٍ**. والسقف الشهري للمستخدم يُفحص مرّة قبل الدور وعدّاده مُخزَّن 30 ثانية، فطلبات متزامنة تقرأ نفس الرقم القديم وتمرّ كلها | M | فاتورة بلا سقف على مستوى المنصّة |
| **A.4** | **الاستدعاء الفاشل يُكلّف ولا يُسجَّل**: مسار الخطأ يُرجع `tokens: 0` بعد أن دُفع ثمن دورات الحلقة | S | تسرّب كلفة صامت |
| **A.5** | **عزل النصّ غير الموثوق عن التعليمات.** نصّ المستخدم ومحتوى الأدوات — ومنها **عناوين مفقودات كتبها طلاب آخرون** — يُحقن مباشرة في الطلب بلا محدّدات | L | حقن أوامر مخزَّن |
| **A.6** | **تأكيد بشري قبل كل أداة كاتبة.** `create_support_ticket` و`report_lost_item` تكتبان صفوفاً بقرار النموذج وحده، بلا تأكيد ولا مفتاح تعافي ولا سقف لكل دور | M | إغراق بتذاكر وتقارير مؤلَّفة |
| **A.7** | **افصل قرار النموذج عن التنفيذ.** `severity: critical` من تصنيف الشكوى **يُجمّد حساباً** — و`confidence` يُرجَع ولا يُفحص أبداً، والقيمة لا تُطابَق مع enum | M | تجميد حساب بجملة داخل نصّ شكوى |
| **A.8** | **طبقة تعمية قبل المزوّد.** `accountSnapshot` يرسل **رصيد المحفظة والنقاط واسم الخطّة وموعد الرحلة القادمة** في كل دور؛ والتحقّق البصري يرفع **صور إيصالات CliQ** (أسماء وأرقام حسابات ومبالغ). لا تعمية ولا قائمة سماح ولا إعداد عدم-احتفاظ | L | إفصاح المرحلة 2 غير مطابق للواقع |
| **A.9** | **`ai_messages.content` نصّ صريح غير مشفَّر**، وسجلّات الأخطاء تحتوي **متن ردّ المزوّد** | M | محتوى محادثات على القرص وفي السجلّات |
| **A.10** | **الاحتفاظ يمحو بيانات الاستخدام** — `RetentionPolicy` يقلّم `ai_messages`، وهي المصدر الوحيد لأرقام الكلفة | S | تاريخ الفاتورة يتبخّر |
| **A.11** | **المساعد يتجاهل `users.locale`** — التعليمة الجذرية ورسائل التعذّر عربية مثبَّتة. ومفتاح التخزين المؤقّت لا يطبّع العربية، فـ`كيف` و`كيــف` مدخلان منفصلان | M | مستخدم إنجليزي يُجاب بالعربية |
| **A.12** | **لا أثر تدقيقي لاستدعاءات الأدوات** — كل مسار كاتب آخر في المشروع يُدقَّق، إلا هذا | M | «مَن طلب من النموذج فتح هذه التذكرة؟» بلا جواب |
| **A.13** | **صفر اختبار للطبقة الشبكية** لـ`OpenAiGptClient`، ولا للتخزين المؤقّت، ولا اختبار واحد يؤكّد أنّ النموذج **لا يمكن إقناعه** باستدعاء أداة كاتبة | L | انحدار صامت في مسار مدفوع |
| **A.14** | **مهلة الطلب غير محدودة فعلياً**: 60 ثانية × 4 دورات ≈ 4 دقائق، بلا إعادة محاولة ولا نموذج بديل | S | طلب معلَّق ومستخدم ينتظر |

## ثانياً: التوسيع — بعد أن صار محسوباً

| # | المهمة | حجم |
|---|---|---|
| **B.1** | **مساعد الكابتن** — «كم ربحت هذا الأسبوع؟» · «ليش ما وصلني عرض؟» · «شو ديني عليّ؟». الأدوات الستّ كلها طالبية اليوم | M |
| **B.2** | **شرح الأجرة بالنصّ** — «ليش 1.500؟» يجيب: النطاق C، هذا المسار، هذه التعرفة المعتمدة، ونسختها. يُبنى على `band` و`tariff_version` من المرحلة 5 | S |
| **B.3** | **تنبؤ الطلب لكل مسار** — أي (منطقة↔جامعة) سيمتلئ في أي نافذة، لتوجيه الكباتن **قبل** الذروة. مدخل مباشر لتقليل مدفوعات الضمان | L |
| **B.4** | **اقتراح فتح منطقة جديدة** — تجميع رفض `UNPRICED_CORRIDOR` يرسم خريطة الطلب غير الملبّى: أي مسار يطلبه طلاب كثيرون ولا سعر له. أرخص بحث سوق ممكن | M |
| **B.5** | **مساعد الأدمن على البيانات** — سؤال بالعربية ← استعلام محدود القراءة على قائمة سماح، لا SQL حرّ | L |
| **B.6** | **كشف تلاعب المسار** — مقارنة أثر GPS بالمسار المتوقّع؛ يُغني `GpsFraudService` القائم | M |
| **B.7** | **ردّ أول مقترَح للدعم** — الاقتراح موجود في `ai_triage` ولا يُعرض للموظّف | S |
| **B.8** | **صوت عربي للكابتن** — أثناء السَوق، اليدان على المقود. يتطلّب A.11 أولاً | L |

**🚪 بوابة 11.5:** كل استدعاء مدفوع مُقيَّد ومحدود ومُدقَّق · سقف عام يعمل · صفر بيانات
شخصية بلا قائمة سماح · اختبار حقن أخضر · صفر أداة كاتبة بلا تأكيد.

> **لماذا التصليب قبل التوسيع:** كل بند في «التوسيع» يضيف استدعاءات مدفوعة. إضافة
> B.3 فوق A.2 المفتوح تعني مضاعفة فاتورة لا نعرف رقمها. وإضافة B.5 فوق A.5 المفتوح
> تعني منح حقن الأوامر منفذاً إلى بيانات الإدارة.

---

# 🚀 المرحلة 12 — الإطلاق · 5 أيام

| # | المهمة |
|---|---|
| 12.1 | Staging مطابق للإنتاج + seed واقعي |
| 12.2 | **بيتا مغلقة**: 20 طالباً + 5 كباتن في إربد · أسبوع · قناة ملاحظات |
| 12.3 | إفصاح خصوصية المتجرين (Data Safety · App Privacy) مطابقاً للواقع بعد المرحلة 2 |
| 12.4 | صفحات المتجر — اللقطات تُولَّد من `docs/design/src` |
| 12.5 | مراجعة داخلية لكل بنود المتاجر |
| 12.6 | Sentry + تنبيهات (SOS مفتوح · SLA المدفوعات · فشل `scheduler`) |
| 12.7 | Runbook + خطة تراجع **مُختبَرة** |
| 12.8 | **إطلاق تدريجي: اليرموك أولاً** → أسبوعان → توسّع |

---

## الجدول الزمني

| المرحلة | المدة | مانعة للإطلاق |
|---|---|---|
| 0 — المنجز | ✅ | — |
| 1 — إسعاف مالي وأمني | ✅ **مكتملة** | — |
| 2 — الامتثال والقانون | ✅ **مكتملة** | — |
| 3 — الخصوصية والدفتر | ✅ **مكتملة** | ✅ |
| 4 — الحذف | ✅ **مكتملة** | ✅ |
| 5 — التسعير | ✅ **مكتملة** | ✅ |
| 6 — التوكنز | ✅ **مكتملة** | ✅ |
| 7 — المكوّنات | 5 أيام | ✅ |
| 8 — الطالب | 5 أيام | ✅ |
| 9 — الكابتن | 5 أيام | ✅ |
| 10 — الإدارة | 5 أيام | — |
| 11 — التصليب | 6 أيام | ✅ |
| **11.5 — الذكاء الاصطناعي** | **6 أيام** | ✅ (التصليب فقط) |
| 12 — الإطلاق | 5 أيام | ✅ |

**≈ 50 يوم عمل متبقّية ≈ 10 أسابيع** (أُنجزت المراحل 0–6).

**المسار الحرج:** `7 → 8 → 9 → 11 → 11.5 → 12`
المرحلة **10** بالتوازي مع 11 · **التصليب في 11.5 (A.1–A.14) مانع للإطلاق؛ التوسيع
(B.1–B.8) ليس كذلك** ويمكن أن يتبع الإطلاق.

---

## مؤشّرات النجاح

| المؤشّر | البداية | الآن | الهدف |
|---|---|---|---|
| CI على `main` | 🔴 | **🟢** ✅ | 🟢 |
| بيئة اختبار | SQLite (تُبطل الأقفال) | **Postgres** ✅ | Postgres |
| الاختبارات | 222 | **443 + 48 واجهة** ✅ | — |
| أخطاء PHPStan | 216 | **0** ✅ | 0 |
| **الدفتر متوازن** (قيود = أرصدة) | ✗ العمولة تتبخّر | **✅ محفظة خزينة** | ✅ |
| **مضاعف الذروة (surge)** | يُحمّل الطالب | **✅ محذوف** | 0 |
| **مقاعد بسعر لم يعتمده أحد** | تسعير بالمسافة | **✅ 0 — مرفوض في المدخلين** | 0 |
| **مقابض تسعير لا تفعل شيئاً** | 9 من 13 | **✅ 0 (4 مقابض)** | 0 |
| **مسارات AI مدفوعة بلا حدّ** | 4 | **✅ 0** | 0 |
| **سقف عام لكلفة AI** | ✗ إعداد ميت | **✅ مفروض** | ✅ |
| **مسارات مال بلا قفل** | 5 | **✅ 0** | 0 |
| **شاشات تعرض ٠ عند فشل الشبكة** | 8 | **✅ 0** | 0 |
| **أسطح إدارية على الدور وحده** | 4 | **✅ 0** | 0 |
| **حالات لا يُخرجها شيء** | 3 | **✅ 0** (`expire-stale`) | 0 |
| **تدقيق إلزامي على خلق الرصيد** | ✗ يُبلَع | **✅ `logOrFail`** | ✅ |
| استدعاءات AI مدفوعة غير مُقيَّدة في الدفتر | 6 من 7 | 6 من 7 | **0** (A.2) |
| شاشات مبنيّة بلا رابط يوصل إليها | ? | **1** (`invoices`) | **0** (م٩) |
| نصوص عربية مثبَّتة في لوحة الأدمن | ~190 | ~190 | **0** (١٠.٥) |
| الفرع الافتراضي | `foundation` | **`main`** ✅ | `main` |
| فروع عن بُعد | 66 | **9** ✅ | ≤5 |
| ملفات الوثائق | 20 | **5** ✅ | ≤6 |
| وزن أصول ميتة | 7.4 MB | **0** ✅ | 0 |
| ألوان hex يدوية | 117 (المقيس **140**) | **74** — ميزانية تُسدَّد في ٨·٩ | **0** |
| خرق RTL | 388 (المقيس **10**) | **10** — ميزانية | **0** |
| `dark:` | 57 (المقيس **0**) | **0** ✅ صفر صلب | **0** |
| أسطر مكرّرة | 962 | 962 | **0** |
| موديولات | 33 | 33 | **20** |
| دوال API بلا مستدعي | 38 | 38 | **0** |
| شاشات ميتة | 7 | 7 | **0** |
| `accessibilityLabel` | 0/154 | 0/154 | **154/154** |
| اختبارات فرونت | 0 | **48** (`shared` 28 · `tokens` 20) | **≥40** ✅ |
| تغطية الباكند | **غير مقيسة** | غير مقيسة | **≥60% مقيسة** |
| موديولات بلا اختبار | 5 | 5 | **0** |
| مسارات مالية بلا قفل | 5 | 5 | **0** |
| أدوار تكسر فصل الواجبات | 1 | 1 | **0** |
| التزامات احتفاظ | 2/6 | 2/6 | **6/6** |
| نسخ احتياطي مُختبَر | ✗ | ✗ | **شهري** |
| بنود امتثال مفتوحة | 10 | 10 | **0** |
| معالجات فرعية مُفصَحة | 0/4 | 0/4 | **4/4** |

---

## ما ينتظر قرارك

| # | القرار | يحجب |
|---|---|---|
| 1 | ~~7 أرقام التسعير~~ — **محسومة ومنفَّذة** في [`product/PRICING.md`](product/PRICING.md) §7 | ~~المرحلة 5~~ ✅ |
| 2 | **مراجعة قانونية أردنية** للوثيقتين بعد إعادة كتابتهما — **القرار الوحيد المتبقّي** | بوابة المرحلة 2 |

> القرارات ٣–٥ (تمويل الخزينة · تسلسل فتح المناطق · الاحتفاظ بمحادثات المساعد)
> **حُسمت ونُفِّذت** — التفصيل أدناه.

---

## القرارات الثلاثة المحسومة

### ١. تمويل الخزينة عند الإطلاق → **عوّامة افتتاحية ١٥٠ د.أ، مُوثَّقة**

**المعطى:** الضمان يُدفع من الخزينة، و`WalletService::apply()` يرفض أي رصيد سالب — فالمنصّة
لا تستطيع دعم أكثر مما جنته. وهذا مقصود. لكنّ نتيجته أنّ الخزينة **صفر صبح أول يوم**، فأوّل
الرحلات الناقصة لا تُدعَم.

**القرار:** تُموَّل بعوّامة افتتاحية عبر أمر مخصّص:

```bash
php artisan rafeeq:fund-treasury 150 --reference=CLIQ-2026-08-01-OPENING
```

**لماذا التمويل لا التعطيل:** الرحلات الناقصة الأولى تخصّ **كباتن التجربة الخمسة** (بند
12.2) — وهم الأكثر عرضة للنقص لأنّ العرض والطلب كليهما في أدنى مستوى عند البداية. تشغيل
المنصّة والشبكة مطفأة في اللحظة التي تُحتاج فيها أكثر **يعاقب المبكّرين على تبكيرهم**، ولو
سائا اثنان من الخمسة انتهت التجربة.

**لماذا ١٥٠ تحديداً:** الرحلة الأسوأ حالاً (راكب واحد، نطاق C) تكلّف **٢.٢٢٥** دعماً.
فـ١٥٠ د.أ = **٦٧ ضماناً في أسوأ الحالات** — أضعاف ما يحتمله أسبوع أول بمقياس ٢٠ طالباً
و٥ كباتن، وفي الوقت نفسه رقم تافه في ميزانية إطلاق.

**والأهم — تُسجَّل كرأس مال لا كـ«تسوية»:** الأمر **يفرض** `--reference` ويمرّ عبر
`adminTopup` (متعدّد الاستدعاء آمن + تدقيق إلزامي عبر `logOrFail`). هذا المكان الوحيد الذي
يدخل فيه مال لحساب المنصّة بلا رحلة خلفه، ولازم يبدو كما هو: مبلغ، ومرجع بنكي، وقيد تدقيق.

### ٢. أي مناطق تُفتح أولاً → **اليرموك وحدها، والأحياء داخل ٣ كم**

**القرار:** الموجة الأولى = **اليرموك × {وسط البلد · الحي الشرقي · الحي الجنوبي · النزهة}**
— أربعة مسارات، كلّها **نطاق A بـ١.٠٠٠ للمقعد**. الأربعة والعشرون كلّها **مُسعَّرة**، وعشرون
منها **مُغلقة** (`is_active = false`).

**لماذا الكثافة لا التغطية:** المنتج **تجميع**، والتجميع يحتاج ثلاثة ركّاب على **نفس المسار
في نفس النافذة**. مئتا طالب موزّعون على ٢٤ مساراً = ثمانية لكل مسار يومياً — **لا تملأ
مركبة أبداً**، فتنطلق كل رحلة ناقصة ويُدفع الضمان على كلّها. نفس المئتين على أربعة مسارات
**يتجمّعون فعلاً**.

**لماذا اليرموك:** ~٣٥ ألف طالب، وموقعها قريب من وسط إربد — أي أنّ الطلب موجود أصلاً.
([Times Higher Education](https://www.timeshighereducation.com/world-university-rankings/yarmouk-university) ·
[IELTS](https://www.ielts.org/about-ielts/who-accepts-ielts-scores/ro/yarmouk-university))

**لماذا نطاق A أولاً:** ١.٠٠٠ للمقعد مقابل تكسي مباشر بـ٣–٤ د.أ. هذه **أحدّ صيغة للعرض**،
وأول فوج يجب أن يسمع أحدّ صيغة.

**لماذا JUST مؤجَّلة (وليست مرفوضة):** على ~١٥ كم باتجاه الرمثا، فتقع في نطاقَي E–F
(٢.٠٠٠–٢.٢٥٠)، والكابتن ينجز رحلات أقلّ بالساعة، و**الرحلة الطويلة الناقصة هي أغلى رحلة
تدعمها المنصّة**. هي الخطوة الثانية الصحيحة، لا الأولى.

**والفائدة التقنية:** الرفض صار **صادقاً** («ما فتحنا هذه المنطقة بعد») لا عَرَضياً، وفتح
مسار صار **علامة واحدة في شاشة الأدمن** لا نشرة كود.

### ٣. الاحتفاظ بمحادثات المساعد → **٣٠ يوماً كما هي، لكن مشفَّرة**

**المعطى:** `RetentionPolicy` يقلّم `ai_messages` بعد **٣٠ يوماً** و`ai_conversations` بعد
٦٠ — وهي مدّة قصيرة ومبرَّرة أصلاً («المحادثة راحة لا سجلّ»). فالمدّة لم تكن المشكلة.

**المشكلة الحقيقية:** `ai_messages.content` كان **نصّاً صريحاً غير مشفَّر**، بينما المرحلة ٣
شفّرت الأسماء والهواتف والبريد والرقم الوطني. وهذا العمود أكشف ما في المخطّط: الطالب يكتب
بكلماته عن **ماله** («ليش رصيدي ناقص؟») و**تحرّكاته** («رحلتي بكرا الصبح») و**شكواه من
كابتن باسمه**.

**القرار:** المدّة تبقى ٣٠ يوماً — التشفير ليس سبباً للاحتفاظ أطول — و`content` صار
`'encrypted'`. نسخة من قاعدة البيانات بلا مفتاح التطبيق **لا تُقرأ كمفكّرة**.

**ما بقي مفتوحاً وسُجِّل صراحة:** التقليم يمحو أرقام الاستخدام معه، لأنّ `ai_messages.tokens`
هو المصدر الوحيد للكلفة. فصل حساب الكلفة عن محتوى الرسائل هو **البند A.2/A.10** في المرحلة
11.5، وبناء جدول تجميعي نصف بناء أسوأ من تسجيله كعمل معلوم.

---

## الملاحق
- [`AUDIT.md`](AUDIT.md) — التدقيق الشامل (5 جولات)
- [`product/PRICING.md`](product/PRICING.md) — نموذج التسعير
- [`design/SCREENS.md`](design/SCREENS.md) — 41 واجهة
- [`design/README.md`](design/README.md) — الهوية
- [`design/src/`](design/src/) — مصادر الهوية القابلة لإعادة التوليد
