# خطة العمل الرسمية — رفيق

> **المصدر الوحيد للحقيقة.** أي عمل لا يوجد هنا لا يُنفَّذ.
> تحلّ محلّ [`00-MASTER-PLAN.md`](00-MASTER-PLAN.md) وتدمج نتائج **أربع جولات تدقيق**.
> **الحالة: بدأنا.** المرحلة 0 جارية — أول بندين مُنجزان ✅

---

## القرارات المعتمدة

| # | القرار | الحالة |
|---|---|---|
| 1 | **مسار الرحلة:** `RideRequests` + `Matching` هو المنتج الأساسي (طلب فوري). `Trips` المجدولة تُعاد تأطيرها كـ**«خطّي الشهري»** بتدفّق منفصل مدخله «حسابي ← اشتراكي» | ✅ معتمد |
| 2 | **الهوية:** «مسار» — أزرق `#1259E3`، بلا لون ثانوي، فاتح فقط، `IBM Plex Sans Arabic` بثلاثة أوزان | ✅ معتمد |
| 3 | **الشعار:** «الطريق هو الحرف» — القفلة **(أ)** علامة + كلمة منفصلتان | ✅ معتمد |
| 4 | **Splash:** الفاتح (لا وميض · يبدو أسرع · يطابق الهوية) | ✅ معتمد |
| 5 | **سايدبار الأدمن:** فاتح | ✅ معتمد |
| 6 | **الكثافة:** المقياس المكثَّف (رقم بطل 26 · زر 46 · هامش 16 · تبويب 64) بحدّ لمس 44+ و54 في وضع رحلة الكابتن | ✅ معتمد |
| 7 | **الوضع الداكن:** يُحذف بالكامل في المرحلة الأولى | ✅ معتمد |
| 8 | **الأيقونات:** Lucide حصراً في التطبيقات الثلاثة | ✅ معتمد |
| 9 | **السنّ الأدنى** | ⚠️ **بانتظار قرارك** — قرار عمل وقانون. الخطة تفترض **16 سنة حدّاً أدنى + منع من هم تحتها**، ويُعدَّل فوراً إن قرّرت غير ذلك |
| 10 | **الإلغاء بعد الصعود:** بعد `capture` **لا إلغاء** — بل «مشكلة في الرحلة» تفتح نزاعاً يقرّره الأدمن | ✅ معتمد (توصيتي) |

---

## القواعد الحاكمة

| # | القاعدة |
|---|---|
| 1 | **فرع واحد: `main`.** كل عمل في `feat/`·`fix/`·`chore/`·`docs/` وينتهي بـ PR وحذف تلقائي |
| 2 | **رقم واحد للمهام: GitHub Issue.** لا `RFQ-###` جديد. RFQ‑366 هو الأخير |
| 3 | **CI أخضر إلزامي للدمج** — بلا استثناء، بلا `--no-verify` |
| 4 | **لا مرحلة تبدأ قبل إغلاق بوابة خروج التي قبلها** |
| 5 | **الحذف قبل الإضافة** — أي ميزة جديدة تحتاج مبرّراً في Issue |
| 6 | **لا كود ميت «لاحقاً»** — إما موصول أو محذوف |
| 7 | **كل إصلاح مالي أو أمني يحتاج اختباراً يفشل قبله وينجح بعده** |

---

# ✅ المرحلة 0 — الإسعاف · جارية

## المُنجز

| # | العمل | النتيجة |
|---|---|---|
| 0.1 | **إصلاح الـ CI الأحمر** — `admin-dashboard` كان يطلب `@types/react ^18.3.3` والجذر يثبّت `18.2.79` عبر `overrides`؛ والنطاق غير محقَّق فأبقى npm نسخة ثانية متداخلة (`18.3.31`)، وأنواع React 19 أضافت `bigint` إلى `ReactNode` فتوقّف التعريفان عن التوافق. + تحويل CI وصورة الأدمن إلى `npm ci` | **PR #66 مدموج · `main` أخضر بالكامل أول مرة** ✅ |
| 0.2 | **ترتيب الجيت** — ضمّ `foundation/phase-0-1` إلى `main`، وحذف **54 فرعاً مدموجاً** (من 66 إلى 9) | ✅ |

## ⚠️ يحتاج نقرة واحدة منك

**تغيير الفرع الافتراضي إلى `main`** — حاولتُه فأعاد **403**؛ البوابة تمنع تعديل إعدادات الريبو رغم أن التوكن يقول `admin:true`.
`Settings ← General ← Default branch ← main`

وهناك **أتمتة تُنشئ PRs من `main` إلى `foundation` باستمرار** (#63 · #65 · #67) — يجب إيقافها بعد تغيير الافتراضي، وإلا تعود عقدة الجذعين.

## المتبقّي في المرحلة 0

### 0.أ — أمان وخصوصية فورية

| # | العمل | المرجع |
|---|---|---|
| 0.3 | **احذف `otp_debug` من كل الاستجابات** · `app()->isProduction()` بدل مقارنة `APP_ENV` النصية · الافتراضي `false` · احذف `universal_code` | [`00`](../audit/00-FULL-AUDIT.md) §2 |
| 0.4 | **لا تسجّل نصّ رسالة SMS** واستثنِ `LogSmsGateway` من fallback الإنتاج | `00` §2 |
| 0.5 | **`lockForUpdate` في `PayoutService::approve/reject`** | `00` §2 |
| 0.6 | **أوقف بثّ موقع الكابتن عند الخروج** + امسح العلم من AsyncStorage | [`02`](../audit/02-MONEY-TIME-CLIENT.md) ق4 |
| 0.7 | **توكن الأدمن إلى `httpOnly` cookie** بدل `localStorage` | `02` ق5 |
| 0.8 | **افصل مفتاح خرائط العميل عن السيرفر** + قيّده في Google Cloud | `00` §2 |
| 0.9 | **سقف 20 ديناراً للاعتماد الآلي** لدفعات CliQ | `00` §2 |
| 0.10 | **نظّف `.env.example`** (`APP_DEBUG=false` · أسرار `CHANGE_ME`) | `00` §2 |
| 0.11 | **احذف `devLogin`** من كود الإنتاج | `00` §2 |

### 0.ب — المال (خسارة جارية الآن)

| # | العمل | المرجع |
|---|---|---|
| 0.12 | **`cancelBooking` يفكّ الحجز المالي** — انقله إلى الخدمة داخل transaction + `lockForUpdate`، وأعِد `RideRequest` إلى `Pending`، وأعِد رصيد الاشتراك | [`01`](../audit/01-LAUNCH-BLOCKERS.md) ق1 |
| 0.13 | **امنع إلغاء رحلة بعد أول `capture`** + احرس ضد الإلغاء المتكرر | `01` ق2 |
| 0.14 | **رحلة بلا كابتن = خطأ صريح** لا ائتمان صامت بصفر | `02` ق2 |
| 0.15 | **طبّع المنطقة الزمنية عند الإدخال** — `Carbon::parse($v)->setTimezone(config('app.timezone'))` | `02` ق1 |
| 0.16 | **`lockForUpdate` في كل موديول `Trips`** (السعة · الصعود · البدء/الإنهاء) | `01` خ1·خ2·خ5 |
| 0.17 | **قفل `SubscriptionService::payWithWallet`** + مسار احتياطي للمحفظة عند انتهاء الاشتراك | `01` خ3·خ4 |

### 0.ج — الامتثال (روافض مؤكّدة)

| # | العمل |
|---|---|
| 0.18 | **ترقية Expo SDK 51 ← 54+** (targetSdk 35) · تثبيت `expo-updates` · `versionCode`/`buildNumber` · معالجة `newArchEnabled` |
| 0.19 | **`extra.apiUrl` إنتاجي** في `eas.json` بدل `http://localhost:8000` |
| 0.20 | **شاشة حذف الحساب** + `anonymizeAccount()` حقيقي |
| 0.21 | **أيقونة 1024** + `splash.image` + `adaptiveIcon.foregroundImage` |
| 0.22 | **انشر الخصوصية والشروط على URL عام** واربطها فعلياً |
| 0.23 | **`locationWhenInUse`** بدل `Always` + مبرّرات الكاميرا/الصور بالعربي + `android.permissions` صريحة |
| 0.24 | **`expo-location` في `driver-app`** — مثبّتة ومعلَنة (تتبّع الكابتن معطّل صامتاً اليوم) |
| 0.25 | **حقل تاريخ الميلاد** + سنّ أدنى في الشروط ← *بانتظار القرار 9* |
| 0.26 | **اربط `/admin/safety/*`** — بلاغات SOS لا يراها أحد اليوم |
| 0.27 | **احذف البيانات الوهمية** — «٣ دقائق» · السيارة الوهمية · أشرطة KPI الثابتة |

### 🚪 بوابة الخروج 0
- [x] `main` أخضر بالكامل
- [ ] لا يمكن الحصول على OTP من الشبكة أو اللوجات
- [ ] `approve`+`reject` متزامنان ⇒ أحدهما يفشل (اختبار concurrency)
- [ ] إلغاء حجز يفكّ الحجز المالي (اختبار)
- [ ] رحلة مجدولة 21:30 تُقرأ 21:30 (اختبار منطقة زمنية)
- [ ] `eas build --profile production` يُنتج تطبيقاً يتصل بالـAPI الحقيقي
- [ ] حذف الحساب يعمل ويُجهِّل الهوية
- [ ] بلاغ SOS يظهر في الأدمن

---

# المرحلة 1 — الخصوصية وسلامة الدفتر · 5 أيام

| # | العمل | المرجع |
|---|---|---|
| 1.1 | **أصلح الاحتساب المزدوج** — رحلة الاشتراك لا تُقيَّد كعمولة رحلة، والكوبون يُفسَّر في التقرير | `02` ق3 |
| 1.2 | **`restrictOnDelete` على كل الجداول المالية** بدل `cascade` على `users` | `02` خ1 |
| 1.3 | **`CHECK (x >= 0)`** على كل أعمدة المال | `02` خ5 |
| 1.4 | **`PruneDriverLocations`** (7 أيام) + إصلاح `PruneTripTracking` ليشمل الرحلات العالقة | `00` §خصوصية |
| 1.5 | **`PruneDriverDocuments`** — حذف عند الرفض/الاستقالة/حذف الحساب | `00` |
| 1.6 | **`PruneTickets`** و**`PrunePayments`** — التزامان معلنان وغير منفَّذين | `00` |
| 1.7 | **`ExpireSubscriptions`** + فهرس على `ends_at` + `endOfDay` | `02` خ2 |
| 1.8 | **تنقيح الإيصال قبل OpenAI** + موافقة صريحة + إفصاح كمعالج فرعي + إلغاء الرابط العام | `00` |
| 1.9 | **تشفير الهاتف/الاسم/العناوين** + فهرس hash للبحث | `00` |
| 1.10 | **نسخ احتياطي `pg_dump` يومي + خارج الموقع + اختبار استعادة شهري مؤتمَت** | `00` |
| 1.11 | **healthcheck حقيقي للـ`scheduler`** (اليوم `\|\| exit 0` = لا يفشل أبداً) + تنبيه `failed_jobs` | `01` م1·م2 |
| 1.12 | **راجع المستندات القانونية الأربعة** لتطابق الكود بالحرف |

**🚪 بوابة:** كل التزام في `data-retention-ar.md` له job، واستعادة نسخة احتياطية **مُختبَرة فعلياً مرة على الأقل**.

---

# المرحلة 2 — الحذف الكبير · 5 أيام

**33 موديولاً ← 20 · 61 شاشة ← 41**

| ما يُحذف | الحجم |
|---|---|
| `Modules/Areas` + `Modules/PickupPoints` | ~1336 سطر · صفر مستهلك |
| `student: payments · addresses` · `driver: invoices` | 516 سطر شاشات ميتة |
| `ServiceTile` · `POST /wallet/topup-instructions` · `AdBanner` من الرئيسية | — |
| اللوحة الداكنة + 57 `dark:` + أسماء `dbg/dcard/dtext` | — |
| الأسماء المستعارة `gold`·`cyan`·`blue`·`navy` | 48 استخداماً |
| **38 دالة api-client بلا مستدعي** | — |
| تعليقات الهويات الميتة (`Onyx v7` · `ink` · `gold accent`) | — |

**يُؤجَّل** إلى `parking/marketplace`: `Exchange` · `Parcels` · `LostFound`
**يُدمَج:** `Complaints` ← `Support` (بعد إكمال `support.show`/`reply` — الطالب اليوم يفتح تذكرة ولا يقرأ الردّ)
**يُوصَل:** `PATCH/DELETE /driver/vehicles/{id}` · `ratings.mine/received`

**🚪 بوابة:** صفر شاشة بلا مسار وصول · صفر دالة API بلا مستدعي (سكربت في CI) · 20 موديولاً

---

# المرحلة 3 — التوكنز · 5 أيام

`packages/tokens` من `docs/design/src/kit.css` ← يولّد TS للجوال + CSS للأدمن + Tailwind preset.
حذف `packages/shared/src/theme/**` · 3 أوزان خط فقط (**حذف `extrabold` الوهمي**) · Lucide في الثلاثة.

### 🔒 بوابات CI الست
| # | البوابة | الخرق اليوم |
|---|---|---|
| 1 | أي `#RRGGBB`/`rgba(` في `app/**` أو `components/**` | **117** |
| 2 | حجم خط خارج المقياس | **7** |
| 3 | `row-reverse` أو `textAlign:'right'` | **388** |
| 4 | `dark:` في الأدمن | **57** |
| 5 | انحراف `tokens.json` عن المولَّد | — |
| 6 | تباين أي زوج < 4.5 للنصوص | **1** (`warning` عند 2.35) |

---

# المرحلة 4 — مكتبة المكوّنات · 5 أيام

`packages/ui` — نقل `LiveMap` **مرة واحدة** (510 سطر ×2) · `Feedback` · `Loader` · `Screen` · `Icon` · توحيد 9 ملفات منحرفة · توحيد `lib/api` و`store/auth` و`i18n`.

**يُضاف داخل المكتبة:**
- `Text` هو المكوّن الوحيد للنص + ESLint يمنع `<Text>` الخام
- `Button` من 6 variants ← 4
- L0–L3: **حدّ أو ظل، أبداً الاثنان**
- **`accessibilityLabel` إلزامية** على كل زر أيقونة (اليوم صفر على 154 عنصراً)
- **`KeyboardAvoidingView`** في كل شاشة بحقول
- حالات `loading`·`empty`·`error`·`offline` لكل قائمة
- **معالجة مركزية لـ403/422/500** + timeout منفصل للرفع + retry
- احترام `reduce-motion`

**🚪 بوابة:** صفر ملف مكوّن مكرّر · كل زر أيقونة له تسمية

---

# المرحلة 5 — تطبيق الطالب · 5 أيام
**19 ← 12 شاشة · 4 تبويبات** — المرجع: `docs/design/v2/02·03·04`
زر AI يخرج من مركز التبويب ← حبّة على الرئيسية · شيت واحد بثلاث نقاط · `emergency` يُبسّط من 340 سطراً · إكمال نوع `Wallet` بحقول التوفّر.

# المرحلة 6 — تطبيق الكابتن · 5 أيام
**14 ← 8 شاشات** — المرجع: `docs/design/v2/05`
مفتاح متصل بعرض الشاشة · العرض الوارد ملء الشاشة + عدّاد + صوت · وضع الرحلة بلا تمرير وأهداف 54 · **«الرصيد المتاح» يعرض المتاح فعلاً** · كود إنزال 6 أرقام + throttle.

# المرحلة 7 — لوحة الإدارة · 5 أيام
**28 ← 18 صفحة · 6 ← 4 مجموعات** — المرجع: `docs/design/v2/06-admin-1/2/3`
سايدبار فاتح · رأس جدول فاتح · command palette واحدة · `tabular-nums` · `ms/me/start/end` · **سقف `per_page`=100** · `<th scope>` · **i18n حقيقي** (اليوم عربي مثبّت).

# المرحلة 8 — التصليب · 6 أيام
`throttle:auth` مطبّع + قفل حساب · توكن 7 أيام + refresh + `abilities` · CORS · **`Policy` + `scopeBindings`** (صفر Policy اليوم) · reverse proxy + TLS · `ENTRYPOINT` في Dockerfile · حدود موارد.
**الاختبارات:** توكنز · تباين · Vitest للمكوّنات (**صفر اختبار فرونت اليوم**) · 3 مسارات E2E · لقطات بصرية · **اختبار concurrency لكل مسار مالي**.

# المرحلة 9 — الإطلاق · 5 أيام
Staging · بيتا مغلقة (20 طالباً + 5 كباتن في إربد) · إفصاح خصوصية المتجرين · صفحات المتجر (اللقطات من `docs/design/src`) · Sentry + تنبيهات · Runbook · rollback مُختبَر · **إطلاق تدريجي: اليرموك أولاً**.

---

## الجدول

| المرحلة | المدة | مانعة للإطلاق |
|---|---|---|
| 0 — الإسعاف | 6 أيام | ✅ |
| 1 — الخصوصية والدفتر | 5 أيام | ✅ |
| 2 — الحذف | 5 أيام | ✅ |
| 3 — التوكنز | 5 أيام | ✅ |
| 4 — المكوّنات | 5 أيام | ✅ |
| 5 — الطالب | 5 أيام | ✅ |
| 6 — الكابتن | 5 أيام | ✅ |
| 7 — الإدارة | 5 أيام | — |
| 8 — التصليب | 6 أيام | ✅ |
| 9 — الإطلاق | 5 أيام | ✅ |

**≈ 52 يوم عمل ≈ 11 أسبوعاً.**

---

## مؤشّرات النجاح

| المؤشّر | البداية | الآن | الهدف |
|---|---|---|---|
| CI على `main` | 🔴 أحمر | **🟢 أخضر** ✅ | أخضر |
| فروع عن بُعد | 66 | **9** ✅ | ≤5 |
| جذوع متنازعة | 2 | 2 *(بانتظار نقرتك)* | 1 |
| ألوان hex يدوية | 117 | 117 | **0** |
| خرق RTL | 388 | 388 | **0** |
| `dark:` | 57 | 57 | **0** |
| أسطر مكرّرة | 962 | 962 | **0** |
| موديولات | 33 | 33 | **20** |
| دوال API بلا مستدعي | 38 | 38 | **0** |
| شاشات ميتة | 7 | 7 | **0** |
| `accessibilityLabel` | 0 / 154 | 0 / 154 | **154 / 154** |
| اختبارات فرونت | 0 | 0 | **≥40** |
| التزامات احتفاظ منفَّذة | 2/6 | 2/6 | **6/6** |
| جداول تتبّع بلا سقف | 1 | 1 | **0** |
| نسخ احتياطي مُختبَر | ✗ | ✗ | **شهري مؤتمَت** |
| بنود امتثال مفتوحة | 10 | 10 | **0** |
| مسارات مالية بلا قفل | 5 | 5 | **0** |

---

## الملاحق

- [`../audit/00-FULL-AUDIT.md`](../audit/00-FULL-AUDIT.md) — التصميم · الأمان · المزايا · الجيت
- [`../audit/01-LAUNCH-BLOCKERS.md`](../audit/01-LAUNCH-BLOCKERS.md) — منطق الأعمال · سلسلة الإصدار · القاصرون
- [`../audit/02-MONEY-TIME-CLIENT.md`](../audit/02-MONEY-TIME-CLIENT.md) — المال · الوقت · قاعدة البيانات · أمن العميل
- [`../design/SCREENS.md`](../design/SCREENS.md) — 41 واجهة
- [`../design/README.md`](../design/README.md) — الهوية
- [`../design/src/`](../design/src/) — مصادر الهوية القابلة لإعادة التوليد
