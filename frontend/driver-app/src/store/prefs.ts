import { createPrefsStore } from '@rafeeq/ui';

/** Language + onboarding flag. The captain onboarding is a different five screens. */
export const usePrefs = createPrefsStore('rafeeq_driver_prefs');
